local Players             = game:GetService("Players")
local RunService          = game:GetService("RunService")
local UserInputService    = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local TweenService        = game:GetService("TweenService")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local Rayfield = loadstring(game:HttpGet(
    "https://sirius.menu/rayfield"
))()

local keySpam         = false
local spamDelay       = 0.12
local deathTP         = false
local lastDeathCFrame
local loopGoto        = false
local loopTargetName  = ""
local autoReset       = false
local resetDelay      = 2
local m1Spam          = false
local m1Delay         = 0.1
local xczEnabled      = false
local xczCooldown     = false
local sideDash        = false
local sideDashLoaded  = false
local altBalantTarget = ""
local altBalantActive = false
local altBalantBusy   = false

local Window = Rayfield:CreateWindow({
    Name             = "🔥 MOLTEN HUB",
    Icon             = 0,
    LoadingTitle     = "MOLTEN HUB",
    LoadingSubtitle  = "THE STRONGEST BATTLEGROUNDS",
    Theme            = "Dark",
    DisableRayfieldPrompts = false,
    DisableBuildWarnings   = false,
    ConfigurationSaving = {
        Enabled    = true,
        FolderName = "MoltenHub",
        FileName   = "MoltenHub_Config",
    },
    Discord = {
        Enabled = false,
    },
    KeySystem = false,
})

local MovementTab = Window:CreateTab("Movement", 4483362458)
local CombatTab   = Window:CreateTab("Combat",   4483345998)
local UtilityTab  = Window:CreateTab("Utility",  4483362458)

-- ══════════════════════════════════════════
--  MOVEMENT TAB
-- ══════════════════════════════════════════

MovementTab:CreateSection("Side Dash Assist")

MovementTab:CreateToggle({
    Name         = "🔥 Molten Dash",
    CurrentValue = false,
    Flag         = "SideDash",
    Callback = function(val)
        sideDash = val
        if val and not sideDashLoaded then
            sideDashLoaded = true
            task.spawn(function()

                local splash = Instance.new("ScreenGui", playerGui)
                splash.ResetOnSpawn = false
                splash.IgnoreGuiInset = true
                splash.DisplayOrder = 999

                local bg = Instance.new("Frame", splash)
                bg.Size = UDim2.fromScale(1, 1)
                bg.BackgroundColor3 = Color3.fromRGB(20, 0, 40)
                bg.BorderSizePixel = 0
                bg.BackgroundTransparency = 1

                local grad = Instance.new("UIGradient", bg)
                grad.Color = ColorSequence.new({
                    ColorSequenceKeypoint.new(0,   Color3.fromRGB(76,  0,  130)),
                    ColorSequenceKeypoint.new(0.3, Color3.fromRGB(138, 43, 226)),
                    ColorSequenceKeypoint.new(0.6, Color3.fromRGB(75,  0,  130)),
                    ColorSequenceKeypoint.new(1,   Color3.fromRGB(148, 0,  211)),
                })
                grad.Rotation = 45

                local title = Instance.new("TextLabel", bg)
                title.Size = UDim2.fromScale(1, 0.25)
                title.Position = UDim2.fromScale(0, 0.28)
                title.BackgroundTransparency = 1
                title.Text = "MOLTEN DASH TECH"
                title.Font = Enum.Font.GothamBold
                title.TextScaled = true
                title.TextColor3 = Color3.new(1, 1, 1)
                title.TextTransparency = 1

                local v3Label = Instance.new("TextLabel", bg)
                v3Label.Size = UDim2.fromScale(1, 0.12)
                v3Label.Position = UDim2.fromScale(0, 0.53)
                v3Label.BackgroundTransparency = 1
                v3Label.Text = "V3"
                v3Label.Font = Enum.Font.GothamBold
                v3Label.TextScaled = true
                v3Label.TextColor3 = Color3.fromRGB(200, 100, 255)
                v3Label.TextTransparency = 1

                local credit = Instance.new("TextLabel", bg)
                credit.Size = UDim2.fromScale(1, 0.07)
                credit.Position = UDim2.fromScale(0, 0.66)
                credit.BackgroundTransparency = 1
                credit.Text = "Made by Molten 🔥"
                credit.Font = Enum.Font.Code
                credit.TextScaled = true
                credit.TextColor3 = Color3.fromRGB(180, 100, 255)
                credit.TextTransparency = 1

                local barBg = Instance.new("Frame", bg)
                barBg.Size = UDim2.new(0.6, 0, 0, 6)
                barBg.Position = UDim2.new(0.2, 0, 0.8, 0)
                barBg.BackgroundColor3 = Color3.fromRGB(60, 0, 100)
                barBg.BorderSizePixel = 0
                barBg.BackgroundTransparency = 1
                Instance.new("UICorner", barBg).CornerRadius = UDim.new(1, 0)

                local barFill = Instance.new("Frame", barBg)
                barFill.Size = UDim2.fromScale(0, 1)
                barFill.BackgroundColor3 = Color3.fromRGB(180, 0, 255)
                barFill.BorderSizePixel = 0
                Instance.new("UICorner", barFill).CornerRadius = UDim.new(1, 0)

                local conn
                conn = RunService.Heartbeat:Connect(function()
                    if not splash or not splash.Parent then
                        conn:Disconnect()
                        return
                    end
                    grad.Rotation = (grad.Rotation + 0.5) % 360
                    local hue = tick() * 0.25 % 1
                    title.TextColor3 = Color3.fromHSV(hue, 0.4, 1)
                    v3Label.TextColor3 = Color3.fromHSV((hue + 0.3) % 1, 0.6, 1)
                    barFill.BackgroundColor3 = Color3.fromHSV((hue + 0.6) % 1, 1, 1)
                    credit.TextColor3 = Color3.fromHSV((hue + 0.5) % 1, 0.5, 1)
                end)

                TweenService:Create(bg, TweenInfo.new(0.4), {BackgroundTransparency = 0}):Play()
                task.wait(0.3)
                TweenService:Create(barBg, TweenInfo.new(0.3), {BackgroundTransparency = 0}):Play()
                TweenService:Create(title, TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {TextTransparency = 0}):Play()
                task.wait(0.35)
                TweenService:Create(v3Label, TweenInfo.new(0.6, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {TextTransparency = 0}):Play()
                task.wait(0.3)
                TweenService:Create(credit, TweenInfo.new(0.4), {TextTransparency = 0}):Play()

                TweenService:Create(barFill, TweenInfo.new(2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.fromScale(1, 1)}):Play()
                task.wait(2)

                loadstring(game:HttpGet(
                    "https://raw.githubusercontent.com/Cyborg883/FakeDash/refs/heads/main/Protected_5833389828844912.lua"
                ))()

                TweenService:Create(bg, TweenInfo.new(0.6), {BackgroundTransparency = 1}):Play()
                TweenService:Create(title, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
                TweenService:Create(v3Label, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
                TweenService:Create(credit, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
                TweenService:Create(barBg, TweenInfo.new(0.6), {BackgroundTransparency = 1}):Play()
                task.wait(0.7)
                conn:Disconnect()
                splash:Destroy()

            end)
        elseif val then
            Rayfield:Notify({
                Title    = "Molten Dash",
                Content  = "Already loaded! 🔥",
                Duration = 2,
                Image    = 4483362458,
            })
        end
    end,
})

MovementTab:CreateSection("Goto / Teleport")

MovementTab:CreateInput({
    Name                     = "Target Username",
    PlaceholderText          = "Enter player name...",
    RemoveTextAfterFocusLost = false,
    Callback = function(val)
        loopTargetName = val
    end,
})

MovementTab:CreateToggle({
    Name         = "Loop Goto",
    CurrentValue = false,
    Flag         = "LoopGoto",
    Callback = function(val)
        loopGoto = val
    end,
})

MovementTab:CreateSection("Character")

MovementTab:CreateToggle({
    Name         = "Auto Reset",
    CurrentValue = false,
    Flag         = "AutoReset",
    Callback = function(val)
        autoReset = val
    end,
})

MovementTab:CreateSlider({
    Name         = "Reset Delay (sec)",
    Range        = {0.05, 10},
    Increment    = 0.05,
    Suffix       = "s",
    CurrentValue = 2,
    Flag         = "ResetDelay",
    Callback = function(val)
        resetDelay = val
    end,
})

MovementTab:CreateToggle({
    Name         = "Death TP",
    CurrentValue = false,
    Flag         = "DeathTP",
    Callback = function(val)
        deathTP = val
    end,
})

-- ══════════════════════════════════════════
--  COMBAT TAB
-- ══════════════════════════════════════════

CombatTab:CreateSection("Key Spam")

CombatTab:CreateToggle({
    Name         = "Key Spam",
    CurrentValue = false,
    Flag         = "KeySpam",
    Callback = function(val)
        keySpam = val
    end,
})

CombatTab:CreateSlider({
    Name         = "Spam Delay (sec)",
    Range        = {0.02, 1},
    Increment    = 0.01,
    Suffix       = "s",
    CurrentValue = 0.12,
    Flag         = "SpamDelay",
    Callback = function(val)
        spamDelay = val
    end,
})

CombatTab:CreateSection("M1 / Punch Spam")

CombatTab:CreateToggle({
    Name         = "M1 Spam",
    CurrentValue = false,
    Flag         = "M1Spam",
    Callback = function(val)
        m1Spam = val
    end,
})

CombatTab:CreateSlider({
    Name         = "M1 Delay (sec)",
    Range        = {0.02, 1},
    Increment    = 0.01,
    Suffix       = "s",
    CurrentValue = 0.1,
    Flag         = "M1Delay",
    Callback = function(val)
        m1Delay = val
    end,
})

CombatTab:CreateSection("Techniques")

CombatTab:CreateToggle({
    Name         = "⚔️ XCZ Tech  (Press E)",
    CurrentValue = false,
    Flag         = "XCZTech",
    Callback = function(val)
        xczEnabled = val
    end,
})

CombatTab:CreateSection("Scripts")

CombatTab:CreateButton({
    Name     = "👊 Auto Uppercut All",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Auto Uppercut All",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483345998,
            })
            loadstring(game:HttpGet(
                "https://raw.githubusercontent.com/gamerscripter90/Jisj/main/autouppercutallcharacter.lua.txt"
            ))()
        end)
    end,
})

CombatTab:CreateButton({
    Name     = "⚡ True Instant Twisted",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "True Instant Twisted",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483345998,
            })
            loadstring(game:HttpGet(
                "https://api.getpolsec.com/scripts/hosted/2c88af57351658d20eb90be4bbb187e8b6226ea564ff80131ca9fdc96a4d58eb.lua"
            ))()
        end)
    end,
})

CombatTab:CreateButton({
    Name     = "💨 BOOMY LoopDash V2",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "BOOMY LoopDash V2",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483345998,
            })
            loadstring(game:HttpGet(
                "https://api.getpolsec.com/scripts/hosted/84e2bd29cccc0f5302267e4dc952cff6816db4af36416cbd477daaa26d60863d.lua"
            ))()
        end)
    end,
})

CombatTab:CreateButton({
    Name     = "🛡️🔥 Molten Auto Block",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Molten Auto Block",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483345998,
            })
            loadstring(game:HttpGet(
                "https://raw.githubusercontent.com/Cyborg883/CombatGUI/refs/heads/main/TSBCombatGUI"
            ))()
        end)
    end,
})

CombatTab:CreateButton({
    Name     = "💧 Dripz V2",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Dripz V2",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483345998,
            })
            loadstring(game:HttpGet(
                "https://raw.githubusercontent.com/letuankiet09812345-cmyk/Testkiba/refs/heads/main/4cbcd9b905ee3e41.lua.txt"
            ))()
        end)
    end,
})

CombatTab:CreateButton({
    Name     = "🗡️ Kiba Tech",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Kiba Tech",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483345998,
            })
            loadstring(game:HttpGet(
                "https://raw.githubusercontent.com/Defy-cloud/Scripts/refs/heads/main/KibaTech"
            ))()
        end)
    end,
})

CombatTab:CreateButton({
    Name     = "🖤🍪 Oreo Dash",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Oreo Dash",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483345998,
            })
            loadstring(game:HttpGet(
                "https://raw.githubusercontent.com/letuankiet09812345-cmyk/Testkiba/refs/heads/main/Oreo%20Tech%20Script%20By%20YQANTG.txt"
            ))()
        end)
    end,
})

CombatTab:CreateButton({
    Name     = "🌸 Kyoto Tech",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Kyoto Tech",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483345998,
            })
            loadstring(game:HttpGet(
                "https://raw.githubusercontent.com/Cyborg883/KyotoTechRework/refs/heads/main/Protected_9378660372508532.lua"
            ))()
        end)
    end,
})

CombatTab:CreateButton({
    Name     = "🌀 Floater Lethal",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Floater Lethal",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483345998,
            })
            loadstring(game:HttpGet(
                "https://api.jnkie.com/api/v1/luascripts/public/a23acf82fb18b827dca096e149ab0272fc74ea9bb8153cd43e44555acb943c86/download"
            ))()
        end)
    end,
})

-- ══════════════════════════════════════════
--  UTILITY TAB
-- ══════════════════════════════════════════

UtilityTab:CreateSection("Teleports")

UtilityTab:CreateButton({
    Name     = "📍 Teleport to Spot",
    Callback = function()
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp  = char:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(438.84, 439.51, -375.48) * CFrame.Angles(0, 0.69, 0)
        end
    end,
})

UtilityTab:CreateButton({
    Name     = "📍 Teleport 2",
    Callback = function()
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp  = char:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(-129.75, 439.51, -352.24) * CFrame.Angles(-3.14, -0.83, -3.14)
        end
    end,
})

UtilityTab:CreateButton({
    Name     = "📍 Teleport 3",
    Callback = function()
        local char = player.Character or player.CharacterAdded:Wait()
        local hrp  = char:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.CFrame = CFrame.new(1070.00, 406.00, 23014.00) * CFrame.Angles(-3.14, 0.12, -3.14)
        end
    end,
})

UtilityTab:CreateSection("Scripts")

UtilityTab:CreateButton({
    Name     = "🌙 Void Kill",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Void Kill",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483362458,
            })
            loadstring(game:HttpGet(
                "https://raw.githubusercontent.com/Reallyools/Reallyools/refs/heads/main/VoidKill2.1"
            ))()
        end)
    end,
})

UtilityTab:CreateSection("Cosmetics")

UtilityTab:CreateButton({
    Name     = "💀 Headless",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Headless",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483362458,
            })
            loadstring(game:HttpGet(
                "https://rawscripts.net/raw/Universal-Script-Fe-Headless-122249"
            ))()
        end)
    end,
})

UtilityTab:CreateButton({
    Name     = "🦴 Korblox",
    Callback = function()
        task.spawn(function()
            Rayfield:Notify({
                Title    = "Korblox",
                Content  = "Made by Molten 🔥",
                Duration = 3,
                Image    = 4483362458,
            })
            loadstring(game:HttpGet(
                "https://rawscripts.net/raw/Universal-Script-Korblox-All-136498"
            ))()
        end)
    end,
})

-- ══════════════════════════════════════════
--  MAIN BLATANT
-- ══════════════════════════════════════════

UtilityTab:CreateSection("Main Blatant")

UtilityTab:CreateButton({
    Name     = "🔥 Main Blatant",
    Callback = function()
        task.spawn(function()

            local splash = Instance.new("ScreenGui", playerGui)
            splash.ResetOnSpawn = false
            splash.IgnoreGuiInset = true
            splash.DisplayOrder = 999

            local bg = Instance.new("Frame", splash)
            bg.Size = UDim2.fromScale(1, 1)
            bg.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
            bg.BorderSizePixel = 0
            bg.BackgroundTransparency = 1

            local grad = Instance.new("UIGradient", bg)
            grad.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0,    Color3.fromRGB(20,  0,   0)),
                ColorSequenceKeypoint.new(0.25, Color3.fromRGB(180, 0,   0)),
                ColorSequenceKeypoint.new(0.5,  Color3.fromRGB(255, 69,  0)),
                ColorSequenceKeypoint.new(0.75, Color3.fromRGB(180, 0,   0)),
                ColorSequenceKeypoint.new(1,    Color3.fromRGB(20,  0,   0)),
            })
            grad.Rotation = 135

            local fireTop = Instance.new("TextLabel", bg)
            fireTop.Size = UDim2.fromScale(1, 0.08)
            fireTop.Position = UDim2.fromScale(0, 0.05)
            fireTop.BackgroundTransparency = 1
            fireTop.Text = "🔥  🔥  🔥  🔥  🔥"
            fireTop.TextScaled = true
            fireTop.Font = Enum.Font.GothamBold
            fireTop.TextColor3 = Color3.new(1, 1, 1)
            fireTop.TextTransparency = 1

            local moltenTitle = Instance.new("TextLabel", bg)
            moltenTitle.Size = UDim2.fromScale(1, 0.2)
            moltenTitle.Position = UDim2.fromScale(0, 0.18)
            moltenTitle.BackgroundTransparency = 1
            moltenTitle.Text = "MOLTEN"
            moltenTitle.Font = Enum.Font.GothamBold
            moltenTitle.TextScaled = true
            moltenTitle.TextColor3 = Color3.new(1, 1, 1)
            moltenTitle.TextTransparency = 1

            local subTitle = Instance.new("TextLabel", bg)
            subTitle.Size = UDim2.fromScale(1, 0.12)
            subTitle.Position = UDim2.fromScale(0, 0.38)
            subTitle.BackgroundTransparency = 1
            subTitle.Text = "MAIN BLATANT"
            subTitle.Font = Enum.Font.GothamBold
            subTitle.TextScaled = true
            subTitle.TextColor3 = Color3.fromRGB(255, 200, 0)
            subTitle.TextTransparency = 1

            local credit = Instance.new("TextLabel", bg)
            credit.Size = UDim2.fromScale(1, 0.06)
            credit.Position = UDim2.fromScale(0, 0.52)
            credit.BackgroundTransparency = 1
            credit.Text = "Made by Molten 🔥"
            credit.Font = Enum.Font.Code
            credit.TextScaled = true
            credit.TextColor3 = Color3.fromRGB(255, 180, 0)
            credit.TextTransparency = 1

            local infoText = Instance.new("TextLabel", bg)
            infoText.Size = UDim2.fromScale(0.8, 0.05)
            infoText.Position = UDim2.fromScale(0.1, 0.60)
            infoText.BackgroundTransparency = 1
            infoText.Text = "⚡ Auto Farm  •  Anti AFK  •  Anti Mod"
            infoText.Font = Enum.Font.Code
            infoText.TextScaled = true
            infoText.TextColor3 = Color3.fromRGB(255, 255, 100)
            infoText.TextTransparency = 1

            local barBg = Instance.new("Frame", bg)
            barBg.Size = UDim2.new(0.7, 0, 0, 8)
            barBg.Position = UDim2.new(0.15, 0, 0.74, 0)
            barBg.BackgroundColor3 = Color3.fromRGB(40, 10, 0)
            barBg.BorderSizePixel = 0
            barBg.BackgroundTransparency = 1
            Instance.new("UICorner", barBg).CornerRadius = UDim.new(1, 0)

            local barFill = Instance.new("Frame", barBg)
            barFill.Size = UDim2.fromScale(0, 1)
            barFill.BackgroundColor3 = Color3.fromRGB(255, 69, 0)
            barFill.BorderSizePixel = 0
            Instance.new("UICorner", barFill).CornerRadius = UDim.new(1, 0)

            local loadingText = Instance.new("TextLabel", bg)
            loadingText.Size = UDim2.fromScale(1, 0.05)
            loadingText.Position = UDim2.fromScale(0, 0.80)
            loadingText.BackgroundTransparency = 1
            loadingText.Text = "Loading..."
            loadingText.Font = Enum.Font.Code
            loadingText.TextScaled = true
            loadingText.TextColor3 = Color3.fromRGB(255, 150, 0)
            loadingText.TextTransparency = 1

            local fireBottom = Instance.new("TextLabel", bg)
            fireBottom.Size = UDim2.fromScale(1, 0.07)
            fireBottom.Position = UDim2.fromScale(0, 0.88)
            fireBottom.BackgroundTransparency = 1
            fireBottom.Text = "🔥  🔥  🔥  🔥  🔥"
            fireBottom.TextScaled = true
            fireBottom.Font = Enum.Font.GothamBold
            fireBottom.TextColor3 = Color3.new(1, 1, 1)
            fireBottom.TextTransparency = 1

            local conn
            conn = RunService.Heartbeat:Connect(function()
                if not splash or not splash.Parent then
                    conn:Disconnect()
                    return
                end
                grad.Rotation = (grad.Rotation + 0.5) % 360
                local fireHue = 0.02 + math.sin(tick() * 2) * 0.02
                moltenTitle.TextColor3 = Color3.fromHSV(fireHue, 0.3, 1)
                subTitle.TextColor3 = Color3.fromHSV(fireHue + 0.05, 0.7, 1)
                barFill.BackgroundColor3 = Color3.fromHSV(fireHue, 1, 1)
                credit.TextColor3 = Color3.fromHSV(fireHue + 0.08, 0.5, 1)
            end)

            TweenService:Create(bg, TweenInfo.new(0.5), {BackgroundTransparency = 0}):Play()
            task.wait(0.25)
            TweenService:Create(fireTop, TweenInfo.new(0.4), {TextTransparency = 0}):Play()
            task.wait(0.15)
            TweenService:Create(barBg, TweenInfo.new(0.3), {BackgroundTransparency = 0}):Play()
            TweenService:Create(moltenTitle, TweenInfo.new(0.7, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {TextTransparency = 0}):Play()
            task.wait(0.35)
            TweenService:Create(subTitle, TweenInfo.new(0.7, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {TextTransparency = 0}):Play()
            task.wait(0.25)
            TweenService:Create(credit, TweenInfo.new(0.4), {TextTransparency = 0}):Play()
            task.wait(0.2)
            TweenService:Create(infoText, TweenInfo.new(0.4), {TextTransparency = 0}):Play()
            task.wait(0.2)
            TweenService:Create(loadingText, TweenInfo.new(0.3), {TextTransparency = 0}):Play()
            task.wait(0.1)
            TweenService:Create(fireBottom, TweenInfo.new(0.4), {TextTransparency = 0}):Play()

            TweenService:Create(barFill, TweenInfo.new(4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {Size = UDim2.fromScale(1, 1)}):Play()

            task.spawn(function()
                local dots = {"Loading.", "Loading..", "Loading...", "Loading...."}
                local i = 1
                while splash and splash.Parent do
                    loadingText.Text = dots[i]
                    i = i % #dots + 1
                    task.wait(0.4)
                end
            end)

            task.wait(4.5)

            TweenService:Create(bg, TweenInfo.new(0.6), {BackgroundTransparency = 1}):Play()
            TweenService:Create(moltenTitle, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
            TweenService:Create(subTitle, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
            TweenService:Create(credit, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
            TweenService:Create(infoText, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
            TweenService:Create(fireTop, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
            TweenService:Create(fireBottom, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
            TweenService:Create(loadingText, TweenInfo.new(0.6), {TextTransparency = 1}):Play()
            TweenService:Create(barBg, TweenInfo.new(0.6), {BackgroundTransparency = 1}):Play()
            task.wait(0.7)
            conn:Disconnect()
            splash:Destroy()

            -- ✅ LOAD MAIN BLATANT WITH FASTER TP
            local TP_POSITION = Vector3.new(438.84, 439.51, -375.48)
            local TP_ANGLES = CFrame.Angles(0, 0.69, 0)

            -- ✅ Faster teleport loop 0.01s
            task.spawn(function()
                while true do
                    if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                        pcall(function()
                            player.Character.HumanoidRootPart.CFrame = CFrame.new(TP_POSITION) * TP_ANGLES
                        end)
                    end
                    task.wait(0.01)
                end
            end)

            -- Load Phantasm script
            loadstring(game:HttpGet(
                "https://raw.githubusercontent.com/ATrainz/Phantasm/refs/heads/main/Games/TSB.lua"
            ))()

        end)
    end,
})

-- ══════════════════════════════════════════
--  ALT BALANT SECTION
-- ══════════════════════════════════════════

UtilityTab:CreateSection("Alt Balant")

local function getServerPlayers()
    local list = {}
    for _, p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            table.insert(list, p.Name)
        end
    end
    if #list == 0 then
        table.insert(list, "No players found")
    end
    return list
end

local altBalantDropdown = UtilityTab:CreateDropdown({
    Name            = "👥 Alt Balant Player List",
    Options         = getServerPlayers(),
    CurrentOption   = {""},
    MultipleOptions = false,
    Flag            = "AltBalantTarget",
    Callback = function(selected)
        if type(selected) == "table" then
            altBalantTarget = selected[1] or ""
        else
            altBalantTarget = selected or ""
        end
    end,
})

UtilityTab:CreateButton({
    Name     = "🔄 Refresh Player List",
    Callback = function()
        local newList = getServerPlayers()
        altBalantDropdown:Refresh(newList, true)
        Rayfield:Notify({
            Title    = "Alt Balant",
            Content  = "Player list refreshed! (" .. #newList .. " players)",
            Duration = 2,
            Image    = 4483362458,
        })
    end,
})

UtilityTab:CreateToggle({
    Name         = "💥 Alt Balant",
    CurrentValue = false,
    Flag         = "AltBalant",
    Callback = function(val)
        altBalantActive = val
        if val then
            if altBalantTarget == "" or altBalantTarget == "No players found" then
                Rayfield:Notify({
                    Title    = "Alt Balant",
                    Content  = "Please select a target player first!",
                    Duration = 3,
                    Image    = 4483362458,
                })
                altBalantActive = false
                return
            end
            altBalantBusy = false
            task.spawn(function()
                while altBalantActive do
                    if not altBalantBusy then
                        altBalantBusy = true
                        local char = player.Character
                        local myHRP = char and char:FindFirstChild("HumanoidRootPart")
                        local target = Players:FindFirstChild(altBalantTarget)
                        local tgtHRP = target and target.Character and target.Character:FindFirstChild("HumanoidRootPart")
                        if myHRP and tgtHRP then
                            myHRP.CFrame = tgtHRP.CFrame * CFrame.new(0, 0, -2)
                            task.wait(2)
                            if altBalantActive then
                                player:LoadCharacter()
                                player.CharacterAdded:Wait()
                                task.wait(0.5)
                            end
                        else
                            task.wait(0.5)
                        end
                        altBalantBusy = false
                    end
                    task.wait(0.1)
                end
            end)
            Rayfield:Notify({
                Title    = "Alt Balant",
                Content  = "Locked onto: " .. altBalantTarget .. " 🔥",
                Duration = 3,
                Image    = 4483362458,
            })
        else
            altBalantBusy = false
            Rayfield:Notify({
                Title    = "Alt Balant",
                Content  = "Disabled.",
                Duration = 2,
                Image    = 4483362458,
            })
        end
    end,
})

-- ══════════════════════════════════════════
--  XCZ Function
-- ══════════════════════════════════════════
local function doXCZ()
    if xczCooldown then return end
    xczCooldown = true
    local char = player.Character
    if not char then xczCooldown = false return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then xczCooldown = false return end

    VirtualInputManager:SendKeyEvent(true,  Enum.KeyCode.Four, false, game)
    task.wait(0.05)
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Four, false, game)
    task.wait(0.1)
    VirtualInputManager:SendKeyEvent(true,  Enum.KeyCode.W,    false, game)
    task.wait(0.01)
    VirtualInputManager:SendKeyEvent(true,  Enum.KeyCode.Q,    false, game)
    task.wait(0.03)
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.Q,    false, game)
    task.wait(0.04)
    hrp.CFrame = hrp.CFrame * CFrame.Angles(0, math.pi, 0)
    task.wait(0.03)
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.W,    false, game)
    task.wait(0.05)
    VirtualInputManager:SendKeyEvent(true,  Enum.KeyCode.One,  false, game)
    task.wait(0.02)
    VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.One,  false, game)
    task.wait(0.5)
    xczCooldown = false
end

UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.E and xczEnabled then
        task.spawn(doXCZ)
    end
end)

-- ══════════════════════════════════════════
--  M1 Spam Loop
-- ══════════════════════════════════════════
task.spawn(function()
    local punchCount = 0
    while true do
        if m1Spam then
            local char = player.Character
            if char then
                local screenSize = workspace.CurrentCamera.ViewportSize
                local punchX = screenSize.X * 0.93
                local punchY = screenSize.Y * 0.50
                VirtualInputManager:SendMouseButtonEvent(punchX, punchY, 0, true,  game, 1)
                task.wait(0.05)
                VirtualInputManager:SendMouseButtonEvent(punchX, punchY, 0, false, game, 1)
                VirtualInputManager:SendKeyEvent(true,  Enum.KeyCode.One, false, game)
                task.wait(0.02)
                VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.One, false, game)
                punchCount = punchCount + 1
                if xczEnabled and not xczCooldown and punchCount >= 4 then
                    punchCount = 0
                    task.spawn(doXCZ)
                end
            end
            task.wait(m1Delay)
        else
            punchCount = 0
            task.wait(0.05)
        end
    end
end)

-- ══════════════════════════════════════════
--  Loop Goto Heartbeat
-- ══════════════════════════════════════════
RunService.Heartbeat:Connect(function()
    if loopGoto and loopTargetName ~= "" then
        local target = Players:FindFirstChild(loopTargetName)
        if target and target.Character then
            local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")
            local myHRP = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
            if targetHRP and myHRP then
                myHRP.CFrame = targetHRP.CFrame * CFrame.new(0, 0, -2)
            end
        end
    end
end)

-- ══════════════════════════════════════════
--  Key Spam Loop
-- ══════════════════════════════════════════
task.spawn(function()
    while true do
        if keySpam then
            local keys = {
                Enum.KeyCode.One, Enum.KeyCode.Two,
                Enum.KeyCode.Three, Enum.KeyCode.Four,
            }
            for _, k in ipairs(keys) do
                if not keySpam then break end
                VirtualInputManager:SendKeyEvent(true,  k, false, game)
                task.wait(0.02)
                VirtualInputManager:SendKeyEvent(false, k, false, game)
                task.wait(spamDelay)
            end
        else
            task.wait(0.05)
        end
    end
end)

-- ══════════════════════════════════════════
--  Character Setup
-- ══════════════════════════════════════════
local function setupChar(char)
    if not char then return end
    local hum = char:WaitForChild("Humanoid")
    local hrp = char:WaitForChild("HumanoidRootPart")

    if deathTP and lastDeathCFrame then
        task.wait(0.15)
        hrp.CFrame = lastDeathCFrame
    end

    hum.Died:Connect(function()
        lastDeathCFrame = hrp.CFrame
        if autoReset then
            task.wait(resetDelay)
            player:LoadCharacter()
        end
    end)
end

if player.Character then setupChar(player.Character) end
player.CharacterAdded:Connect(setupChar)

-- ══════════════════════════════════════════
--  Load Saved Config
-- ══════════════════════════════════════════
Rayfield:LoadConfiguration()
