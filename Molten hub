local Players             = game:GetService("Players")
local RunService          = game:GetService("RunService")
local UserInputService    = game:GetService("UserInputService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local TweenService        = game:GetService("TweenService")

local player    = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- ══════════════════════════════════════════
--  LOAD WINDUI
-- ══════════════════════════════════════════
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua", true))()

-- ══════════════════════════════════════════
--  STATE VARIABLES
-- ══════════════════════════════════════════
local keySpam         = false
local spamDelay       = 0.12
local deathTP         = false
local lastDeathCFrame
local loopGoto        = false
local loopTargetName  = ""
local autoReset       = false
local resetDelay      = 2
local m1Spam          = false
local m1Delay         = 0.1
local xczEnabled      = false
local xczCooldown     = false
local sideDash        = false
local sideDashLoaded  = false
local altBalantTarget = ""
local altBalantActive = false
local altBalantBusy   = false

-- ══════════════════════════════════════════
--  WINDOW  (purple theme matching photo)
-- ══════════════════════════════════════════
local Window = WindUI:CreateWindow({
    Title       = "🔥 MOLTEN HUB",
    Icon        = "flame",
    Author      = "Molten",
    Folder      = "MoltenHub",
    Size        = UDim2.fromOffset(600, 480),
    Transparent = true,
    Theme       = "Dark",
    Background  = "rbxassetid://130139426964633",
    Acrylic     = true,
    GlassBlur   = 8,
    Border      = true,
    MenuWidth   = 160,
    HasTip      = true,
    -- Purple accent overrides applied via theme below
})
Window:Init()

-- ══════════════════════════════════════════
--  CLOSE BUTTON  (top-right)
-- ══════════════════════════════════════════
-- WindUI ships with a built-in X button; we also add a keybind shortcut
-- and a dedicated button inside Settings so the user can close any time.

-- ══════════════════════════════════════════
--  TABS
-- ══════════════════════════════════════════
local MovementTab  = Window:Tab({ Title = "Movement",  Icon = "footprints" })
local CombatTab    = Window:Tab({ Title = "Combat",    Icon = "sword" })
local UtilityTab   = Window:Tab({ Title = "Utility",   Icon = "wrench" })
local MiscTab      = Window:Tab({ Title = "Misc",      Icon = "package" })
local LocalTab     = Window:Tab({ Title = "Local",     Icon = "user" })
local EspTab       = Window:Tab({ Title = "ESP",       Icon = "eye" })
local DummyTab     = Window:Tab({ Title = "Dummy",     Icon = "bot" })
local TechTab      = Window:Tab({ Title = "Tech",      Icon = "zap" })
local AutoTab      = Window:Tab({ Title = "Auto",      Icon = "refresh-cw" })
local ServerTab    = Window:Tab({ Title = "Server",    Icon = "server" })
local AutoBlockTab = Window:Tab({ Title = "AutoBlock", Icon = "shield" })
local AdminTab     = Window:Tab({ Title = "Admin",     Icon = "crown" })
local SettingsTab  = Window:Tab({ Title = "Settings",  Icon = "settings" })

-- ══════════════════════════════════════════
--  MOVEMENT TAB
-- ══════════════════════════════════════════
local SideDashSection = MovementTab:Section({ Title = "Side Dash Assist", Shown = true })

SideDashSection:Toggle({
    Title   = "🔥 Molten Dash",
    Default = false,
    Callback = function(val)
        sideDash = val
        if val and not sideDashLoaded then
            sideDashLoaded = true
            task.spawn(function()
                local splash = Instance.new("ScreenGui", playerGui)
                splash.ResetOnSpawn = false
                splash.IgnoreGuiInset = true
                splash.DisplayOrder = 999

                local bg = Instance.new("Frame", splash)
                bg.Size = UDim2.fromScale(1,1)
                bg.BackgroundColor3 = Color3.fromRGB(20,0,40)
                bg.BorderSizePixel = 0
                bg.BackgroundTransparency = 1

                local grad = Instance.new("UIGradient", bg)
                grad.Color = ColorSequence.new({
                    ColorSequenceKeypoint.new(0,   Color3.fromRGB(76,0,130)),
                    ColorSequenceKeypoint.new(0.3, Color3.fromRGB(138,43,226)),
                    ColorSequenceKeypoint.new(0.6, Color3.fromRGB(75,0,130)),
                    ColorSequenceKeypoint.new(1,   Color3.fromRGB(148,0,211)),
                })
                grad.Rotation = 45

                local title = Instance.new("TextLabel", bg)
                title.Size = UDim2.fromScale(1,0.25)
                title.Position = UDim2.fromScale(0,0.28)
                title.BackgroundTransparency = 1
                title.Text = "MOLTEN DASH TECH"
                title.Font = Enum.Font.GothamBold
                title.TextScaled = true
                title.TextColor3 = Color3.new(1,1,1)
                title.TextTransparency = 1

                local v3Label = Instance.new("TextLabel", bg)
                v3Label.Size = UDim2.fromScale(1,0.12)
                v3Label.Position = UDim2.fromScale(0,0.53)
                v3Label.BackgroundTransparency = 1
                v3Label.Text = "V3"
                v3Label.Font = Enum.Font.GothamBold
                v3Label.TextScaled = true
                v3Label.TextColor3 = Color3.fromRGB(200,100,255)
                v3Label.TextTransparency = 1

                local credit = Instance.new("TextLabel", bg)
                credit.Size = UDim2.fromScale(1,0.07)
                credit.Position = UDim2.fromScale(0,0.66)
                credit.BackgroundTransparency = 1
                credit.Text = "Made by Molten 🔥"
                credit.Font = Enum.Font.Code
                credit.TextScaled = true
                credit.TextColor3 = Color3.fromRGB(180,100,255)
                credit.TextTransparency = 1

                local barBg = Instance.new("Frame", bg)
                barBg.Size = UDim2.new(0.6,0,0,6)
                barBg.Position = UDim2.new(0.2,0,0.8,0)
                barBg.BackgroundColor3 = Color3.fromRGB(60,0,100)
                barBg.BorderSizePixel = 0
                barBg.BackgroundTransparency = 1
                Instance.new("UICorner", barBg).CornerRadius = UDim.new(1,0)

                local barFill = Instance.new("Frame", barBg)
                barFill.Size = UDim2.fromScale(0,1)
                barFill.BackgroundColor3 = Color3.fromRGB(180,0,255)
                barFill.BorderSizePixel = 0
                Instance.new("UICorner", barFill).CornerRadius = UDim.new(1,0)

                local conn
                conn = RunService.Heartbeat:Connect(function()
                    if not splash or not splash.Parent then conn:Disconnect() return end
                    grad.Rotation = (grad.Rotation + 0.5) % 360
                    local hue = tick() * 0.25 % 1
                    title.TextColor3  = Color3.fromHSV(hue, 0.4, 1)
                    v3Label.TextColor3 = Color3.fromHSV((hue+0.3)%1, 0.6, 1)
                    barFill.BackgroundColor3 = Color3.fromHSV((hue+0.6)%1, 1, 1)
                    credit.TextColor3 = Color3.fromHSV((hue+0.5)%1, 0.5, 1)
                end)

                TweenService:Create(bg,    TweenInfo.new(0.4), {BackgroundTransparency=0}):Play()
                task.wait(0.3)
                TweenService:Create(barBg, TweenInfo.new(0.3), {BackgroundTransparency=0}):Play()
                TweenService:Create(title, TweenInfo.new(0.6,Enum.EasingStyle.Back,Enum.EasingDirection.Out), {TextTransparency=0}):Play()
                task.wait(0.35)
                TweenService:Create(v3Label, TweenInfo.new(0.6,Enum.EasingStyle.Back,Enum.EasingDirection.Out), {TextTransparency=0}):Play()
                task.wait(0.3)
                TweenService:Create(credit, TweenInfo.new(0.4), {TextTransparency=0}):Play()
                TweenService:Create(barFill, TweenInfo.new(2,Enum.EasingStyle.Quad,Enum.EasingDirection.Out), {Size=UDim2.fromScale(1,1)}):Play()
                task.wait(2)

                loadstring(game:HttpGet("https://raw.githubusercontent.com/Cyborg883/FakeDash/refs/heads/main/Protected_5833389828844912.lua"))()

                TweenService:Create(bg,     TweenInfo.new(0.6), {BackgroundTransparency=1}):Play()
                TweenService:Create(title,  TweenInfo.new(0.6), {TextTransparency=1}):Play()
                TweenService:Create(v3Label,TweenInfo.new(0.6), {TextTransparency=1}):Play()
                TweenService:Create(credit, TweenInfo.new(0.6), {TextTransparency=1}):Play()
                TweenService:Create(barBg,  TweenInfo.new(0.6), {BackgroundTransparency=1}):Play()
                task.wait(0.7)
                conn:Disconnect()
                splash:Destroy()
            end)
        elseif val then
            WindUI:Notify({ Title="Molten Dash", Content="Already loaded! 🔥", Duration=2, Icon="check-circle" })
        end
    end,
})

local GotoSection = MovementTab:Section({ Title = "Goto / Teleport", Shown = true })

GotoSection:Input({
    Title       = "Target Username",
    Placeholder = "Enter player name...",
    Default     = "",
    Numeric     = false,
    Finished    = false,
    Callback    = function(val) loopTargetName = val end,
})
GotoSection:Toggle({
    Title   = "Loop Goto",
    Default = false,
    Callback = function(val) loopGoto = val end,
})

local CharSection = MovementTab:Section({ Title = "Character", Shown = true })
CharSection:Toggle({ Title = "Auto Reset",          Default = false, Callback = function(val) autoReset = val end })
CharSection:Slider({ Title = "Reset Delay (sec)",   Default = 2,  Min = 0.05, Max = 10,  Rounding = 2, Callback = function(val) resetDelay = val end })
CharSection:Toggle({ Title = "Death TP",            Default = false, Callback = function(val) deathTP = val end })

-- ══════════════════════════════════════════
--  COMBAT TAB
-- ══════════════════════════════════════════
local KeySpamSection = CombatTab:Section({ Title = "Key Spam", Shown = true })
KeySpamSection:Toggle({ Title = "Key Spam",          Default = false, Callback = function(val) keySpam = val end })
KeySpamSection:Slider({ Title = "Spam Delay (sec)",  Default = 0.12, Min = 0.02, Max = 1, Rounding = 2, Callback = function(val) spamDelay = val end })

local M1Section = CombatTab:Section({ Title = "M1 / Punch Spam", Shown = true })
M1Section:Toggle({ Title = "M1 Spam",         Default = false, Callback = function(val) m1Spam = val end })
M1Section:Slider({ Title = "M1 Delay (sec)",  Default = 0.1,  Min = 0.02, Max = 1, Rounding = 2, Callback = function(val) m1Delay = val end })

local TechSection = CombatTab:Section({ Title = "Techniques", Shown = true })
TechSection:Toggle({ Title = "⚔️ XCZ Tech (Press E)", Default = false, Callback = function(val) xczEnabled = val end })

local CombatScriptsSection = CombatTab:Section({ Title = "Scripts", Shown = true })
local combatButtons = {
    {"👊 Auto Uppercut All",    "https://raw.githubusercontent.com/gamerscripter90/Jisj/main/autouppercutallcharacter.lua.txt"},
    {"⚡ True Instant Twisted", "https://api.getpolsec.com/scripts/hosted/2c88af57351658d20eb90be4bbb187e8b6226ea564ff80131ca9fdc96a4d58eb.lua"},
    {"💨 BOOMY LoopDash V2",    "https://api.getpolsec.com/scripts/hosted/84e2bd29cccc0f5302267e4dc952cff6816db4af36416cbd477daaa26d60863d.lua"},
    {"🛡️🔥 Molten Auto Block",  "https://raw.githubusercontent.com/Cyborg883/CombatGUI/refs/heads/main/TSBCombatGUI"},
    {"💧 Dripz V2",             "https://raw.githubusercontent.com/letuankiet09812345-cmyk/Testkiba/refs/heads/main/4cbcd9b905ee3e41.lua.txt"},
    {"🗡️ Kiba Tech",            "https://raw.githubusercontent.com/Defy-cloud/Scripts/refs/heads/main/KibaTech"},
    {"🖤🍪 Oreo Dash",          "https://raw.githubusercontent.com/letuankiet09812345-cmyk/Testkiba/refs/heads/main/Oreo%20Tech%20Script%20By%20YQANTG.txt"},
    {"🌸 Kyoto Tech",           "https://raw.githubusercontent.com/Cyborg883/KyotoTechRework/refs/heads/main/Protected_9378660372508532.lua"},
    {"🌀 Floater Lethal",       "https://api.jnkie.com/api/v1/luascripts/public/a23acf82fb18b827dca096e149ab0272fc74ea9bb8153cd43e44555acb943c86/download"},
}
for _, data in ipairs(combatButtons) do
    local name, url = data[1], data[2]
    CombatScriptsSection:Button({
        Title = name,
        Callback = function()
            task.spawn(function()
                WindUI:Notify({ Title=name, Content="Made by Molten 🔥", Duration=3, Icon="flame" })
                loadstring(game:HttpGet(url))()
            end)
        end,
    })
end

-- ══════════════════════════════════════════
--  UTILITY TAB
-- ══════════════════════════════════════════
local TPSection = UtilityTab:Section({ Title = "Teleports", Shown = true })
TPSection:Button({ Title = "📍 Teleport to Spot", Callback = function()
    local char = player.Character or player.CharacterAdded:Wait()
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.CFrame = CFrame.new(438.84,439.51,-375.48)*CFrame.Angles(0,0.69,0) end
end})
TPSection:Button({ Title = "📍 Teleport 2", Callback = function()
    local char = player.Character or player.CharacterAdded:Wait()
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.CFrame = CFrame.new(-129.75,439.51,-352.24)*CFrame.Angles(-3.14,-0.83,-3.14) end
end})
TPSection:Button({ Title = "📍 Teleport 3", Callback = function()
    local char = player.Character or player.CharacterAdded:Wait()
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if hrp then hrp.CFrame = CFrame.new(1070.00,406.00,23014.00)*CFrame.Angles(-3.14,0.12,-3.14) end
end})

local UtilScriptsSection = UtilityTab:Section({ Title = "Scripts", Shown = true })
UtilScriptsSection:Button({ Title = "🌙 Void Kill", Callback = function()
    task.spawn(function()
        WindUI:Notify({ Title="Void Kill", Content="Made by Molten 🔥", Duration=3, Icon="moon" })
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Reallyools/Reallyools/refs/heads/main/VoidKill2.1"))()
    end)
end})

local CosmeticsSection = UtilityTab:Section({ Title = "Cosmetics", Shown = true })
CosmeticsSection:Button({ Title = "💀 Headless", Callback = function()
    task.spawn(function()
        WindUI:Notify({ Title="Headless", Content="Made by Molten 🔥", Duration=3, Icon="skull" })
        loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Fe-Headless-122249"))()
    end)
end})
CosmeticsSection:Button({ Title = "🦴 Korblox", Callback = function()
    task.spawn(function()
        WindUI:Notify({ Title="Korblox", Content="Made by Molten 🔥", Duration=3, Icon="bone" })
        loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Korblox-All-136498"))()
    end)
end})

local AltBalantSection = UtilityTab:Section({ Title = "Alt Balant", Shown = true })
local function getServerPlayers()
    local list = {}
    for _,p in ipairs(Players:GetPlayers()) do
        if p ~= player then table.insert(list, p.Name) end
    end
    if #list == 0 then table.insert(list, "No players found") end
    return list
end

local altBalantDropdown = AltBalantSection:Dropdown({
    Title   = "👥 Alt Balant Player List",
    Default = "No players found",
    Options = getServerPlayers(),
    Callback = function(selected)
        altBalantTarget = selected or ""
    end,
})

AltBalantSection:Button({ Title = "🔄 Refresh Player List", Callback = function()
    local newList = getServerPlayers()
    altBalantDropdown:Refresh(newList)
    WindUI:Notify({ Title="Alt Balant", Content="Refreshed! ("..#newList.." players)", Duration=2, Icon="refresh-cw" })
end})

AltBalantSection:Toggle({
    Title   = "💥 Alt Balant",
    Default = false,
    Callback = function(val)
        altBalantActive = val
        if val then
            if altBalantTarget == "" or altBalantTarget == "No players found" then
                WindUI:Notify({ Title="Alt Balant", Content="Select a target player first!", Duration=3, Icon="alert-circle" })
                altBalantActive = false
                return
            end
            altBalantBusy = false
            task.spawn(function()
                while altBalantActive do
                    if not altBalantBusy then
                        altBalantBusy = true
                        local char = player.Character
                        local myHRP = char and char:FindFirstChild("HumanoidRootPart")
                        local target = Players:FindFirstChild(altBalantTarget)
                        local tgtHRP = target and target.Character and target.Character:FindFirstChild("HumanoidRootPart")
                        if myHRP and tgtHRP then
                            myHRP.CFrame = tgtHRP.CFrame*CFrame.new(0,0,-2)
                            task.wait(2)
                            if altBalantActive then
                                player:LoadCharacter()
                                player.CharacterAdded:Wait()
                                task.wait(0.5)
                            end
                        else
                            task.wait(0.5)
                        end
                        altBalantBusy = false
                    end
                    task.wait(0.1)
                end
            end)
            WindUI:Notify({ Title="Alt Balant", Content="Locked onto: "..altBalantTarget.." 🔥", Duration=3, Icon="target" })
        else
            altBalantBusy = false
            WindUI:Notify({ Title="Alt Balant", Content="Disabled.", Duration=2, Icon="x-circle" })
        end
    end,
})

-- ══════════════════════════════════════════
--  MISC TAB
-- ══════════════════════════════════════════
local MiscScriptsSection = MiscTab:Section({ Title = "Scripts", Shown = true })
local miscButtons = {
    {"🎯 M1 Reach",          "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/M1%20Reach%20Rework"},
    {"🔫 Aimbot",             "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Aimlock%20By%20YQANTG"},
    {"🔒 FaceLock",           "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/Tsb/FACELOCK"},
    {"🛡️ Block Aimlock",      "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Block%20Aimlock"},
    {"🌀 Orbit",              "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Orbit%20by%20YQANTG"},
    {"💨 Fake Dash (Q)",      "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Fake%20Dash%20Q"},
    {"⚡ No Slide Dash EndLag","https://raw.githubusercontent.com/Slaphello/No-endlag-side-dash/refs/heads/main/No%20endlag%20side%20dash"},
    {"⌨️ Keyboard",           "https://raw.githubusercontent.com/AZYsGithub/Delta-Scripts/refs/heads/main/MobileKeyboard.txt"},
}
for _, data in ipairs(miscButtons) do
    local name, url = data[1], data[2]
    MiscScriptsSection:Button({
        Title = name,
        Callback = function()
            task.spawn(function()
                WindUI:Notify({ Title=name, Content="Loading...", Duration=2, Icon="loader" })
                loadstring(game:HttpGet(url))()
            end)
        end,
    })
end

-- ══════════════════════════════════════════
--  LOCAL TAB
-- ══════════════════════════════════════════
local LocalCharSection = LocalTab:Section({ Title = "Character", Shown = true })

-- Aura
local auraOn        = false
local auraHighlight = nil
local auraParticles = {}
local auraColorTime = 0
local auraLoop      = nil

local function enableAura()
    local char = player.Character or player.CharacterAdded:Wait()
    local humanoid = char:WaitForChild("Humanoid")
    local anim = Instance.new("Animation")
    anim.AnimationId = "rbxassetid://114586157428274"
    humanoid:LoadAnimation(anim):Play()
    auraHighlight = Instance.new("Highlight")
    auraHighlight.Adornee = char
    auraHighlight.FillTransparency = 1
    auraHighlight.OutlineTransparency = 0
    auraHighlight.OutlineColor = Color3.fromRGB(255,0,255)
    auraHighlight.Parent = char
    for _,partName in pairs({"UpperTorso","LowerTorso","HumanoidRootPart"}) do
        local part = char:FindFirstChild(partName)
        if part then
            local att = Instance.new("Attachment", part)
            local particle = Instance.new("ParticleEmitter", att)
            particle.Name = "DarkAura"
            particle.Rate = 30
            particle.Lifetime = NumberRange.new(1.2,2)
            particle.Speed = NumberRange.new(2,5)
            particle.Size = NumberSequence.new({NumberSequenceKeypoint.new(0,3),NumberSequenceKeypoint.new(1,0)})
            particle.Transparency = NumberSequence.new({NumberSequenceKeypoint.new(0,0.1),NumberSequenceKeypoint.new(1,1)})
            particle.Color = ColorSequence.new(Color3.fromRGB(15,15,15))
            particle.Rotation = NumberRange.new(0,360)
            particle.RotSpeed = NumberRange.new(-90,90)
            particle.SpreadAngle = Vector2.new(180,180)
            particle.ZOffset = -1
            table.insert(auraParticles, particle)
        end
    end
    auraLoop = RunService.RenderStepped:Connect(function(dt)
        if auraOn and auraHighlight then
            auraColorTime += dt*0.25
            local r = math.sin(auraColorTime)*127+128
            local g = math.sin(auraColorTime+2)*127+128
            local b = math.sin(auraColorTime+4)*127+128
            auraHighlight.OutlineColor = Color3.fromRGB(r,g,b)
        end
    end)
end

local function disableAura()
    if auraHighlight then auraHighlight:Destroy() auraHighlight = nil end
    for _,p in pairs(auraParticles) do if p then p:Destroy() end end
    auraParticles = {}
    if auraLoop then auraLoop:Disconnect() auraLoop = nil end
end

LocalCharSection:Toggle({ Title = "✨ Aura (Client)", Default = false, Callback = function(val)
    auraOn = val
    if val then enableAura() else disableAura() end
end})

-- Invisible Block
local blockEnabled   = false
local blockConnection = nil
local blockLoop      = nil
local targetBlockAnimId = "rbxassetid://10470389827"

local function enableInvisBlock()
    local char = player.Character or player.CharacterAdded:Wait()
    local humanoid = char:WaitForChild("Humanoid")
    local function blockAnim(hum)
        for _,track in ipairs(hum:GetPlayingAnimationTracks()) do
            if track.Animation and track.Animation.AnimationId == targetBlockAnimId then
                track:Stop() track:Destroy()
            end
        end
    end
    blockLoop = task.spawn(function()
        while blockEnabled and humanoid and humanoid.Parent do
            blockAnim(humanoid) task.wait(0.05)
        end
    end)
    blockConnection = humanoid.AnimationPlayed:Connect(function(track)
        if track.Animation and track.Animation.AnimationId == targetBlockAnimId then
            track:Stop() track:Destroy()
        end
    end)
end

local function disableInvisBlock()
    if blockConnection then blockConnection:Disconnect() blockConnection = nil end
    if blockLoop then task.cancel(blockLoop) blockLoop = nil end
end

LocalCharSection:Toggle({ Title = "🫥 Invisible Block", Default = false, Callback = function(state)
    blockEnabled = state
    if state then
        enableInvisBlock()
        player.CharacterAdded:Connect(function()
            if blockEnabled then task.wait(1) enableInvisBlock() end
        end)
    else disableInvisBlock() end
end})

LocalCharSection:Toggle({ Title = "🚫 No Stun", Default = false, Callback = function(val)
    if val then
        _G.StunBypass = RunService.RenderStepped:Connect(function()
            local char = player.Character
            if char then
                if char:FindFirstChild("Stunned") then char.Stunned:Destroy() end
                local hum = char:FindFirstChild("Humanoid")
                if hum then hum.PlatformStand = false if hum.WalkSpeed==0 then hum.WalkSpeed=16 end end
            end
        end)
    else
        if _G.StunBypass then _G.StunBypass:Disconnect() _G.StunBypass = nil end
    end
end})

LocalCharSection:Toggle({ Title = "🏃 No Slowed", Default = false, Callback = function(val)
    if val then
        _G.NoSlowLoop = RunService.RenderStepped:Connect(function()
            local char = player.Character
            if char then
                local hum = char:FindFirstChild("Humanoid")
                if hum then
                    if hum.WalkSpeed < 20 then hum.WalkSpeed = 20 end
                    hum.PlatformStand = false
                end
            end
        end)
    else
        if _G.NoSlowLoop then _G.NoSlowLoop:Disconnect() _G.NoSlowLoop = nil end
    end
end})

LocalCharSection:Toggle({ Title = "🚫 No Anim Ult", Default = false, Callback = function(state)
    local blockedIds = {"12447707844","12342141464","12772543293"}
    local function isBlocked(id)
        for _,v in pairs(blockedIds) do if id:find(v) then return true end end
        return false
    end
    if _G.BlockAnimConnection then _G.BlockAnimConnection:Disconnect() _G.BlockAnimConnection = nil end
    if state then
        _G.BlockAnimConnection = RunService.Stepped:Connect(function()
            local char = player.Character
            if not char then return end
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                local animator = hum:FindFirstChildOfClass("Animator")
                if animator then
                    for _,track in ipairs(animator:GetPlayingAnimationTracks()) do
                        local anim = track.Animation
                        if anim and isBlocked(anim.AnimationId) then
                            track:Stop(0) pcall(function() track:Destroy() end)
                        end
                    end
                end
            end
        end)
    end
end})

LocalCharSection:Toggle({ Title = "📷 Face Camera Y", Default = false, Callback = function(enabled)
    if _G.FaceCamConnection then _G.FaceCamConnection:Disconnect() _G.FaceCamConnection = nil end
    if enabled then
        local cam = workspace.CurrentCamera
        _G.FaceCamConnection = RunService.RenderStepped:Connect(function()
            local char = player.Character
            if not char then return end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if not hrp then return end
            local camLook = cam.CFrame.LookVector
            local lookDir = Vector3.new(camLook.X,0,camLook.Z).Unit
            hrp.CFrame = CFrame.new(hrp.Position, hrp.Position+lookDir)
        end)
    end
end})

local LocalOtherSection = LocalTab:Section({ Title = "Other", Shown = true })
LocalOtherSection:Button({ Title = "🔭 Fov", Callback = function()
    task.spawn(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Kakuwhwbbanab"))()
    end)
end})
LocalOtherSection:Toggle({ Title = "💨 Speed", Default = false, Callback = function(state)
    if not _G.MyExternalGUI then
        pcall(function()
            _G.MyExternalGUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Bbbbbbbbbskkakak"))()
        end)
    end
    if typeof(_G.MyExternalGUI)=="Instance" and _G.MyExternalGUI:IsA("ScreenGui") then
        _G.MyExternalGUI.Enabled = state
    end
end})

-- ══════════════════════════════════════════
--  ESP TAB
-- ══════════════════════════════════════════
local PlayerESPSection = EspTab:Section({ Title = "Player ESP", Shown = true })

local espEnabled     = false
local espConnections = {}
local espLabels      = {}
local function createNameESP(target)
    if target == player then return end
    if espLabels[target] then return end
    local head = target:FindFirstChild("Head")
    if not head then return end
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "NameESP"
    billboard.Size = UDim2.new(0,100,0,20)
    billboard.StudsOffset = Vector3.new(0,2.2,0)
    billboard.AlwaysOnTop = true
    billboard.Adornee = head
    billboard.Parent = head
    local nameLabel = Instance.new("TextLabel")
    nameLabel.Size = UDim2.new(1,0,1,0)
    nameLabel.BackgroundTransparency = 1
    nameLabel.Text = target.Name
    nameLabel.TextColor3 = Color3.fromRGB(100,255,255)
    nameLabel.TextStrokeTransparency = 0.4
    nameLabel.TextScaled = true
    nameLabel.Font = Enum.Font.GothamBold
    nameLabel.Parent = billboard
    espLabels[target] = billboard
end
local function enableESP()
    for _,p in ipairs(Players:GetPlayers()) do if p.Character then createNameESP(p.Character) end end
    table.insert(espConnections, Players.PlayerAdded:Connect(function(p)
        p.CharacterAdded:Connect(function(char)
            repeat task.wait() until char:FindFirstChild("Head")
            createNameESP(char)
        end)
    end))
    for _,p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            table.insert(espConnections, p.CharacterAdded:Connect(function(char)
                repeat task.wait() until char:FindFirstChild("Head")
                createNameESP(char)
            end))
        end
    end
end
local function disableESP()
    for _,gui in pairs(espLabels) do if gui then gui:Destroy() end end
    espLabels = {}
    for _,conn in ipairs(espConnections) do if conn then conn:Disconnect() end end
    espConnections = {}
end
PlayerESPSection:Toggle({ Title = "🏷️ ESP Name", Default = false, Callback = function(state)
    espEnabled = state
    if state then enableESP() else disableESP() end
end})

-- Highlight
local highlightEnabled     = false
local highlightConnections = {}
local highlightList        = {}
local function createHighlight(char, plr)
    if plr == player then return end
    if highlightList[plr] then return end
    local hl = Instance.new("Highlight")
    hl.Name = "PlayerHighlight"
    hl.Adornee = char
    hl.FillTransparency = 1
    hl.OutlineTransparency = 0
    hl.OutlineColor = Color3.fromRGB(255,0,0)
    hl.Parent = char
    highlightList[plr] = hl
end
local function enableHL()
    for _,plr in ipairs(Players:GetPlayers()) do
        if plr.Character then createHighlight(plr.Character,plr) end
    end
    table.insert(highlightConnections, Players.PlayerAdded:Connect(function(p)
        table.insert(highlightConnections, p.CharacterAdded:Connect(function(char)
            repeat task.wait() until char:FindFirstChild("HumanoidRootPart")
            createHighlight(char,p)
        end))
    end))
    for _,p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            table.insert(highlightConnections, p.CharacterAdded:Connect(function(char)
                repeat task.wait() until char:FindFirstChild("HumanoidRootPart")
                createHighlight(char,p)
            end))
        end
    end
end
local function disableHL()
    for _,hl in pairs(highlightList) do if hl then hl:Destroy() end end
    highlightList = {}
    for _,conn in ipairs(highlightConnections) do if conn then conn:Disconnect() end end
    highlightConnections = {}
end
PlayerESPSection:Toggle({ Title = "🔴 Highlight", Default = false, Callback = function(state)
    if state then enableHL() else disableHL() end
end})

-- HRP Box
local boxEnabled    = false
local boxList       = {}
local boxConnections = {}
local function createBox(plr)
    if plr == player then return end
    if boxList[plr] then return end
    if not plr.Character then return end
    local hrp = plr.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local box = Instance.new("BoxHandleAdornment")
    box.Name = "PurpleBox"
    box.Adornee = hrp
    box.AlwaysOnTop = true
    box.ZIndex = 5
    box.Size = Vector3.new(2,2,1)
    box.Color3 = Color3.fromRGB(170,0,255)
    box.Transparency = 0.3
    box.Parent = hrp
    boxList[plr] = box
end
local function enableBoxes()
    for _,p in pairs(Players:GetPlayers()) do if p.Character then createBox(p) end end
    table.insert(boxConnections, Players.PlayerAdded:Connect(function(p)
        table.insert(boxConnections, p.CharacterAdded:Connect(function(char)
            repeat task.wait() until char:FindFirstChild("HumanoidRootPart")
            createBox(p)
        end))
    end))
    for _,p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            table.insert(boxConnections, p.CharacterAdded:Connect(function(char)
                repeat task.wait() until char:FindFirstChild("HumanoidRootPart")
                createBox(p)
            end))
        end
    end
end
local function disableBoxes()
    for _,box in pairs(boxList) do if box then box:Destroy() end end
    boxList = {}
    for _,conn in ipairs(boxConnections) do if conn then conn:Disconnect() end end
    boxConnections = {}
end
PlayerESPSection:Toggle({ Title = "🟣 Show HRP Box", Default = false, Callback = function(state)
    boxEnabled = state
    if state then enableBoxes() else disableBoxes() end
end})

-- Show Count
local guiConn    = nil
local countLabel = nil
PlayerESPSection:Toggle({ Title = "🔢 Show Player Count", Default = false, Callback = function(state)
    if state then
        local sg = Instance.new("ScreenGui", playerGui)
        sg.Name = "PlayerCountGui"
        sg.ResetOnSpawn = false
        sg.IgnoreGuiInset = true
        countLabel = Instance.new("TextLabel")
        countLabel.Size = UDim2.new(0,220,0,50)
        countLabel.Position = UDim2.new(0.5,-100,0,50)
        countLabel.BackgroundTransparency = 1
        countLabel.TextColor3 = Color3.fromRGB(0,255,200)
        countLabel.TextStrokeTransparency = 0.2
        countLabel.TextScaled = true
        countLabel.Font = Enum.Font.GothamBlack
        countLabel.TextTransparency = 1
        countLabel.Text = "Players: "..#Players:GetPlayers()
        countLabel.Parent = sg
        TweenService:Create(countLabel, TweenInfo.new(0.6,Enum.EasingStyle.Quad,Enum.EasingDirection.Out), {TextTransparency=0}):Play()
        guiConn = RunService.RenderStepped:Connect(function()
            if countLabel then countLabel.Text = "Players: "..#Players:GetPlayers() end
        end)
    else
        if guiConn then guiConn:Disconnect() guiConn = nil end
        if countLabel then countLabel:Destroy() countLabel = nil end
        local gui = playerGui:FindFirstChild("PlayerCountGui")
        if gui then gui:Destroy() end
    end
end})

local CharESPSection = EspTab:Section({ Title = "Character ESP", Shown = true })
local toolEspMap = {
    ["Flowing Water"]         = {"Garou",         Color3.fromRGB(0,170,255)},
    ["Crushed Rock"]          = {"Garou",          Color3.fromRGB(0,170,255)},
    ["Normal Punch"]          = {"Saitama",        Color3.fromRGB(139,69,19)},
    ["Serious Punch"]         = {"Saitama",        Color3.fromRGB(139,69,19)},
    ["Binding Cloth"]         = {"Monster Form",   Color3.fromRGB(255,0,0)},
    ["Beatdown"]              = {"Brutal Demon",   Color3.fromRGB(255,255,255)},
    ["Death Blow"]            = {"Brutal Demon",   Color3.fromRGB(255,255,255)},
    ["Blitz Shot"]            = {"Cyborg",         Color3.fromRGB(255,140,0)},
    ["Flamewave Cannon"]      = {"Cyborg",         Color3.fromRGB(255,140,0)},
    ["Explosive Shuriken"]    = {"Sonic",          Color3.fromRGB(255,105,180)},
    ["Carnage"]               = {"Sonic",          Color3.fromRGB(255,105,180)},
    ["Atmos Cleave"]          = {"Blade Master",   Color3.fromRGB(255,255,0)},
    ["Atomic Slash"]          = {"Blade Master",   Color3.fromRGB(255,255,0)},
    ["Crushing Pull"]         = {"Tatsumaki",      Color3.fromRGB(0,255,0)},
    ["Psychic Ricochet"]      = {"Tatsumaki",      Color3.fromRGB(0,255,0)},
    ["Bullet Barrage"]        = {"Martial Artist", Color3.fromRGB(128,0,128)},
    ["Earth Splitting Strike"]= {"Martial Artist", Color3.fromRGB(128,0,128)},
}
local espFolder    = Instance.new("Folder", workspace)
espFolder.Name     = "DynamicESP"
local charEspEnabled = false
local function getEspData(p)
    local backpack = p:FindFirstChildOfClass("Backpack")
    if not backpack then return "Tech Prodigy", Color3.fromRGB(200,200,200) end
    for toolName, data in pairs(toolEspMap) do
        if backpack:FindFirstChild(toolName) then return data[1], data[2] end
    end
    return "Tech Prodigy", Color3.fromRGB(200,200,200)
end
local function createCharESP(p, labelText, color)
    local char = p.Character
    if not char or not char:FindFirstChild("Head") then return end
    if espFolder:FindFirstChild(p.Name) then return end
    local billboard = Instance.new("BillboardGui")
    billboard.Name = p.Name
    billboard.Adornee = char.Head
    billboard.Size = UDim2.new(0,100,0,20)
    billboard.StudsOffset = Vector3.new(3.5,0.5,0)
    billboard.AlwaysOnTop = true
    billboard.Parent = espFolder
    local label = Instance.new("TextLabel", billboard)
    label.Size = UDim2.new(1,0,1,0)
    label.BackgroundTransparency = 1
    label.Text = labelText
    label.TextColor3 = color
    label.Font = Enum.Font.GothamBold
    label.TextScaled = false
    label.TextSize = 12
    label.TextStrokeTransparency = 0.6
    label.TextTransparency = 0
end
local function clearCharESP() for _,v in ipairs(espFolder:GetChildren()) do v:Destroy() end end
local function refreshCharESP()
    clearCharESP()
    for _,p in ipairs(Players:GetPlayers()) do
        if p ~= player then
            local lbl, col = getEspData(p)
            createCharESP(p, lbl, col)
        end
    end
end
CharESPSection:Toggle({ Title = "🧍 Esp Character", Default = false, Callback = function(state)
    charEspEnabled = state
    if state then
        refreshCharESP()
        task.spawn(function()
            while charEspEnabled do refreshCharESP() task.wait(2) end
        end)
    else clearCharESP() end
end})

-- ══════════════════════════════════════════
--  DUMMY TAB
-- ══════════════════════════════════════════
local DummySection = DummyTab:Section({ Title = "Dummy Tools", Shown = true })

local npcHighlights = {}
local npcConn = {}
local function highlightNPC(model)
    if npcHighlights[model] then return end
    local hl = Instance.new("Highlight")
    hl.Adornee = model
    hl.FillTransparency = 1
    hl.OutlineTransparency = 0
    hl.OutlineColor = Color3.fromRGB(170,0,255)
    hl.Parent = model
    npcHighlights[model] = hl
end
local function enableNPCHL()
    local live = workspace:FindFirstChild("Live")
    if not live then return end
    for _,m in pairs(live:GetChildren()) do
        if m:IsA("Model") and m.Name=="Weakest Dummy" then highlightNPC(m) end
    end
    table.insert(npcConn, live.ChildAdded:Connect(function(child)
        if child:IsA("Model") and child.Name=="Weakest Dummy" then task.wait(0.2) highlightNPC(child) end
    end))
end
local function disableNPCHL()
    for _,h in pairs(npcHighlights) do if h then h:Destroy() end end
    npcHighlights = {}
    for _,conn in pairs(npcConn) do if conn then conn:Disconnect() end end
    npcConn = {}
end
DummySection:Toggle({ Title = "🟣 Highlight Dummy", Default = false, Callback = function(a)
    if a then enableNPCHL() else disableNPCHL() end
end})

local npcNameTags = {}
local npcNameConn = {}
local function addNameTag(npc)
    if npcNameTags[npc] then return end
    local hrp = npc:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local billboard = Instance.new("BillboardGui")
    billboard.Name = "NameTag"
    billboard.Size = UDim2.new(0,100,0,25)
    billboard.Adornee = hrp
    billboard.AlwaysOnTop = true
    billboard.StudsOffset = Vector3.new(0,3.5,0)
    billboard.Parent = npc
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1,0,1,0)
    lbl.BackgroundTransparency = 1
    lbl.Text = npc.Name
    lbl.TextColor3 = Color3.fromRGB(255,200,0)
    lbl.TextStrokeTransparency = 0.2
    lbl.TextScaled = true
    lbl.Font = Enum.Font.GothamBold
    lbl.Parent = billboard
    npcNameTags[npc] = billboard
end
local function enableNPCNames()
    local live = workspace:FindFirstChild("Live")
    if not live then return end
    for _,npc in pairs(live:GetChildren()) do
        if npc:IsA("Model") and npc.Name=="Weakest Dummy" then addNameTag(npc) end
    end
    table.insert(npcNameConn, live.ChildAdded:Connect(function(npc)
        if npc:IsA("Model") and npc.Name=="Weakest Dummy" then task.wait(0.2) addNameTag(npc) end
    end))
end
local function disableNPCNames()
    for _,tag in pairs(npcNameTags) do if tag then tag:Destroy() end end
    npcNameTags = {}
    for _,conn in ipairs(npcNameConn) do if conn then conn:Disconnect() end end
    npcNameConn = {}
end
DummySection:Toggle({ Title = "🏷️ Show Name", Default = false, Callback = function(state)
    if state then enableNPCNames() else disableNPCNames() end
end})

local orbitConn   = nil
local orbitRadius = 5
local orbitSpeed  = 2
local orbitAngle  = 0
local function getClosestDummy()
    local live = workspace:FindFirstChild("Live")
    if not live then return nil end
    local myChar = player.Character
    if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return nil end
    local myPos = myChar.HumanoidRootPart.Position
    local closest, shortest = nil, math.huge
    for _,npc in pairs(live:GetChildren()) do
        if npc:IsA("Model") and npc.Name=="Weakest Dummy" and npc:FindFirstChild("HumanoidRootPart") then
            local dist = (npc.HumanoidRootPart.Position - myPos).Magnitude
            if dist < shortest then shortest = dist closest = npc end
        end
    end
    return closest
end
DummySection:Toggle({ Title = "🌀 Orbit Dummy", Default = false, Callback = function(a)
    if a then
        orbitAngle = 0
        orbitConn = RunService.RenderStepped:Connect(function(dt)
            local myChar = player.Character
            if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return end
            local myHRP = myChar.HumanoidRootPart
            local npc = getClosestDummy()
            if not npc or not npc:FindFirstChild("HumanoidRootPart") then return end
            local center = npc.HumanoidRootPart.Position
            orbitAngle += dt*orbitSpeed
            local offset = Vector3.new(math.cos(orbitAngle)*orbitRadius, 0, math.sin(orbitAngle)*orbitRadius)
            myHRP.CFrame = CFrame.new(center+offset)*(myHRP.CFrame - myHRP.Position)
        end)
    else
        if orbitConn then orbitConn:Disconnect() orbitConn = nil end
    end
end})

DummySection:Button({ Title = "🌊 Tween to Dummy", Callback = function()
    local myChar = player.Character
    if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return end
    local myHRP = myChar.HumanoidRootPart
    local npc = getClosestDummy()
    if npc and npc:FindFirstChild("HumanoidRootPart") then
        local targetPos = npc.HumanoidRootPart.Position
        local finalPos = Vector3.new(targetPos.X, myHRP.Position.Y, targetPos.Z)
        local currentRot = myHRP.CFrame - myHRP.Position
        TweenService:Create(myHRP, TweenInfo.new(0.6,Enum.EasingStyle.Sine), {CFrame=CFrame.new(finalPos)*currentRot}):Play()
    end
end})

DummySection:Button({ Title = "📍 Teleport to Dummy", Callback = function()
    local myChar = player.Character
    if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return end
    local myHRP = myChar.HumanoidRootPart
    local npc = getClosestDummy()
    if npc and npc:FindFirstChild("HumanoidRootPart") then
        local targetPos = npc.HumanoidRootPart.Position
        local newPos = Vector3.new(targetPos.X, myHRP.Position.Y, targetPos.Z)
        myHRP.CFrame = CFrame.new(newPos)*(myHRP.CFrame - myHRP.Position)
    end
end})

-- ══════════════════════════════════════════
--  TECH TAB
-- ══════════════════════════════════════════
local TechScriptsSection = TechTab:Section({ Title = "Tech Scripts", Shown = true })
local techButtons = {
    {"🔄 M1 Reset",      "https://raw.githubusercontent.com/Slaphello/M1-Reset-And-Emote-Dash-TSB-OLD-/refs/heads/main/M1R%26ED%20TSB"},
    {"🌸 Auto Kyoto",    "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Auto%20kyoto%20ma%20hoa"},
    {"🔁 Loop Dash",     "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Loop%20Dash%20Rework%20Script%20Real"},
    {"💀 Lethal Dash",   "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Lethal%20Dash%20Script"},
    {"🍪 Oreo Dash",     "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Oreo%20Tech%20Script"},
    {"🌪️ Tornado Dash",  "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Idk%20lolololol"},
    {"⚡ Supa Dash",     "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Supa%20tech%20script"},
    {"↩️ BackDash (MB)", "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/BackDash%20Tech"},
    {"↩️ BackDash (PC)", "https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/BackDash%20For%20Pc"},
}
for _,data in ipairs(techButtons) do
    local name, url = data[1], data[2]
    TechScriptsSection:Button({
        Title = name,
        Callback = function()
            task.spawn(function()
                WindUI:Notify({ Title=name, Content="Loading...", Duration=2, Icon="zap" })
                loadstring(game:HttpGet(url))()
            end)
        end,
    })
end

-- ══════════════════════════════════════════
--  AUTO TAB
-- ══════════════════════════════════════════
local AutoFeatSection = AutoTab:Section({ Title = "Auto Features", Shown = true })

-- Auto Twisted
local autoTwistedConn = nil
local autoTwistedChar = nil
AutoFeatSection:Toggle({ Title = "⚡ Auto Twisted", Default = false, Callback = function(enabled)
    local function bindAnimDetect()
        local char = player.Character or player.CharacterAdded:Wait()
        local humanoid = char:WaitForChild("Humanoid")
        local cooldown = false
        local function stepBack()
            local hrp = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
            if hrp then hrp.CFrame = hrp.CFrame*CFrame.new(0,0,3) end
        end
        local function useRemote()
            local c = player.Character
            if c and c:FindFirstChild("Communicate") then
                c.Communicate:FireServer({["Dash"]=Enum.KeyCode.W,["Key"]=Enum.KeyCode.Q,["Goal"]="KeyPress"})
            end
        end
        autoTwistedConn = humanoid.AnimationPlayed:Connect(function(track)
            if track.Animation and track.Animation.AnimationId=="rbxassetid://13294471966" and not cooldown then
                cooldown = true
                task.delay(0.23, function() stepBack() useRemote() end)
                task.delay(5, function() cooldown = false end)
            end
        end)
    end
    if enabled then
        bindAnimDetect()
        autoTwistedChar = player.CharacterAdded:Connect(function() task.wait(1) bindAnimDetect() end)
    else
        if autoTwistedConn then autoTwistedConn:Disconnect() autoTwistedConn = nil end
        if autoTwistedChar then autoTwistedChar:Disconnect() autoTwistedChar = nil end
    end
end})

-- Instant Twisted
local instantTwistedConn = nil
local instantTwistedChar = nil
AutoFeatSection:Toggle({ Title = "🌀 Instant Twisted", Default = false, Callback = function(enabled)
    local cam = workspace.CurrentCamera
    local debounce = false
    local function swipeAndDash()
        local args = {{["Dash"]=Enum.KeyCode.W,["Key"]=Enum.KeyCode.Q,["Goal"]="KeyPress"}}
        local char = player.Character
        if char and char:FindFirstChild("Communicate") then char.Communicate:FireServer(unpack(args)) end
        local startCFrame = cam.CFrame
        local leftCFrame = startCFrame*CFrame.Angles(40,math.rad(-90),180)
        cam.CameraType = Enum.CameraType.Scriptable
        local t1 = TweenService:Create(cam,TweenInfo.new(0.1,Enum.EasingStyle.Sine),{CFrame=leftCFrame})
        t1:Play() t1.Completed:Wait()
        task.wait(0.00001)
        local t2 = TweenService:Create(cam,TweenInfo.new(0.001,Enum.EasingStyle.Sine),{CFrame=startCFrame})
        t2:Play() t2.Completed:Wait()
        task.wait(0.1)
        local t3 = TweenService:Create(cam,TweenInfo.new(0.0001,Enum.EasingStyle.Sine),{CFrame=startCFrame*CFrame.Angles(math.rad(-6),math.rad(20),0)})
        t3:Play() t3.Completed:Wait()
        cam.CameraType = Enum.CameraType.Custom
    end
    local function bindAnimator(animator)
        instantTwistedConn = animator.AnimationPlayed:Connect(function(track)
            if track.Animation and track.Animation.AnimationId=="rbxassetid://13294471966" and not debounce then
                debounce = true
                local char = player.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                if hrp then hrp.CFrame = hrp.CFrame + hrp.CFrame.LookVector*-1 end
                task.delay(0.25, function() swipeAndDash() task.wait(5) debounce = false end)
            end
        end)
    end
    local function monitorChar()
        local char = player.Character or player.CharacterAdded:Wait()
        local humanoid = char:WaitForChild("Humanoid")
        local animator = humanoid:FindFirstChildOfClass("Animator")
        if animator then bindAnimator(animator)
        else humanoid.ChildAdded:Connect(function(child) if child:IsA("Animator") then bindAnimator(child) end end) end
    end
    if enabled then
        monitorChar()
        instantTwistedChar = player.CharacterAdded:Connect(monitorChar)
    else
        if instantTwistedConn then instantTwistedConn:Disconnect() instantTwistedConn = nil end
        if instantTwistedChar then instantTwistedChar:Disconnect() instantTwistedChar = nil end
    end
end})

-- Auto Supa
local autoSupaConn = nil
local autoSupaChar = nil
AutoFeatSection:Toggle({ Title = "⚡ Auto Supa", Default = false, Callback = function(state)
    local rotating = false
    local function bindHumanoid(humanoid)
        autoSupaConn = humanoid.AnimationPlayed:Connect(function(track)
            if track.Animation and track.Animation.AnimationId=="rbxassetid://12296113986" and not rotating then
                rotating = true
                local char = player.Character
                local humanoid2 = char and char:FindFirstChild("Humanoid")
                if humanoid2 then humanoid2.Jump = true end
                task.delay(2, function()
                    local c = player.Character
                    local remote = c and c:FindFirstChild("Communicate")
                    if remote then remote:FireServer({["Dash"]=Enum.KeyCode.W,["Key"]=Enum.KeyCode.Q,["Goal"]="KeyPress"}) end
                    task.delay(0.26, function()
                        local c2 = player.Character
                        local root = c2 and c2:FindFirstChild("HumanoidRootPart")
                        if root then
                            local lookVector = -root.CFrame.LookVector
                            root.CFrame = CFrame.lookAt(root.CFrame.Position, root.CFrame.Position+lookVector)
                        end
                        if humanoid2 then humanoid2.Jump = false end
                        rotating = false
                    end)
                end)
            end
        end)
    end
    local function setup()
        local char = player.Character or player.CharacterAdded:Wait()
        local humanoid = char:WaitForChild("Humanoid")
        bindHumanoid(humanoid)
    end
    if state then
        setup()
        autoSupaChar = player.CharacterAdded:Connect(setup)
    else
        if autoSupaConn then autoSupaConn:Disconnect() autoSupaConn = nil end
        if autoSupaChar then autoSupaChar:Disconnect() autoSupaChar = nil end
    end
end})

-- Slide + M1
local slideM1Conn = nil
local slideM1Char = nil
local slideTargetIDs = {}
slideTargetIDs["10480796021"] = true
slideTargetIDs["10480793962"] = true
AutoFeatSection:Toggle({ Title = "🛹 Slide + M1", Default = false, Callback = function(state)
    if not state then
        if slideM1Conn then slideM1Conn:Disconnect() slideM1Conn = nil end
        if slideM1Char then slideM1Char:Disconnect() slideM1Char = nil end
        return
    end
    local function setup(char)
        local humanoid = char:WaitForChild("Humanoid",3)
        local animator = humanoid and humanoid:FindFirstChildOfClass("Animator")
        local remote = char:FindFirstChild("Communicate")
        if not humanoid or not animator or not remote then return end
        local cooldown = false
        if slideM1Conn then slideM1Conn:Disconnect() end
        slideM1Conn = animator.AnimationPlayed:Connect(function(track)
            local id = track.Animation.AnimationId:match("%d+")
            if id and slideTargetIDs[id] and not cooldown then
                cooldown = true
                remote:FireServer({["Mobile"]=true,["Goal"]="LeftClick"})
                remote:FireServer({["Goal"]="LeftClickRelease",["Mobile"]=true})
                task.delay(1.5, function() cooldown = false end)
            end
        end)
    end
    if player.Character then setup(player.Character) end
    if slideM1Char then slideM1Char:Disconnect() end
    slideM1Char = player.CharacterAdded:Connect(function(char) task.wait(1) if state then setup(char) end end)
end})

-- Auto Surf
local autoSurfConn   = nil
local autoSurfActive = false
AutoFeatSection:Toggle({ Title = "🏄 Auto Surf", Default = false, Callback = function(state)
    autoSurfActive = state
    if not state then
        if autoSurfConn then autoSurfConn:Disconnect() autoSurfConn = nil end
        return
    end
    local isTweening = false
    local TARGET_ANIM = "rbxassetid://12309835105"
    local function isTargetAnimPlaying()
        local char = player.Character
        local humanoid = char and char:FindFirstChildOfClass("Humanoid")
        if not humanoid then return false end
        for _,track in ipairs(humanoid:GetPlayingAnimationTracks()) do
            if track.Animation and track.Animation.AnimationId==TARGET_ANIM then return true end
        end
        return false
    end
    RunService.RenderStepped:Connect(function()
        if not autoSurfActive or isTweening then return end
        if isTargetAnimPlaying() then
            isTweening = true
            task.wait(0.6)
            local char = player.Character
            local root = char and char:FindFirstChild("HumanoidRootPart")
            if root then
                local forward = root.CFrame.LookVector.Unit
                local targetCF = root.CFrame + (forward*50)
                local tween = TweenService:Create(root, TweenInfo.new(0.78,Enum.EasingStyle.Linear), {CFrame=targetCF})
                tween:Play() tween.Completed:Wait()
            end
            task.wait(1.5)
            isTweening = false
        end
    end)
end})

-- Auto DownSlam
local downSlamEnabled = false
AutoFeatSection:Toggle({ Title = "⬇️ Auto DownSlam", Default = false, Callback = function(state)
    downSlamEnabled = state
end})

local downSlamValidIDs = {
    ["rbxassetid://10469639222"]=true,["rbxassetid://13532604085"]=true,
    ["rbxassetid://13295919399"]=true,["rbxassetid://13378751717"]=true,
    ["rbxassetid://14001963401"]=true,["rbxassetid://15240176873"]=true,
    ["rbxassetid://16515448089"]=true,["rbxassetid://17889471098"]=true,
    ["rbxassetid://104895379416342"]=true,
}
local function isNearTarget(hrp)
    for _,model in ipairs(workspace.Live and workspace.Live:GetChildren() or {}) do
        if model:IsA("Model") and model~=player.Character then
            local root = model:FindFirstChild("HumanoidRootPart")
            if root and (root.Position-hrp.Position).Magnitude<=15 then
                if Players:GetPlayerFromCharacter(model) or model.Name=="Weakest Dummy" then return true end
            end
        end
    end
    return false
end
local function setupDownSlam(char)
    local humanoid = char:WaitForChild("Humanoid")
    local hrp      = char:WaitForChild("HumanoidRootPart")
    humanoid.AnimationPlayed:Connect(function(track)
        if downSlamEnabled and track.Animation and downSlamValidIDs[track.Animation.AnimationId] then
            if not isNearTarget(hrp) then return end
            local tween = TweenService:Create(hrp, TweenInfo.new(0.35,Enum.EasingStyle.Quad,Enum.EasingDirection.Out), {CFrame=hrp.CFrame+Vector3.new(0,6,0)})
            tween:Play()
            humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)
end
player.CharacterAdded:Connect(setupDownSlam)
if player.Character then setupDownSlam(player.Character) end

-- Flowing + Grasp
local flowGraspConn = nil
AutoFeatSection:Toggle({ Title = "🌊 Flowing + Grasp", Default = false, Callback = function(enabled)
    if flowGraspConn then flowGraspConn:Disconnect() flowGraspConn = nil end
    if not enabled then return end
    local isTweening = false
    local lastPlaying = false
    flowGraspConn = RunService.RenderStepped:Connect(function()
        local char = player.Character
        local humanoid = char and char:FindFirstChild("Humanoid")
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not humanoid or not hrp then return end
        local isPlaying = false
        for _,track in ipairs(humanoid:GetPlayingAnimationTracks()) do
            if track.Animation and track.Animation.AnimationId=="rbxassetid://12273188754" then isPlaying=true break end
        end
        if isPlaying and not isTweening and not lastPlaying then
            isTweening = true lastPlaying = true
            task.delay(1.8, function()
                local fwdCF = hrp.CFrame + hrp.CFrame.LookVector*24
                local tween = TweenService:Create(hrp, TweenInfo.new(0.1,Enum.EasingStyle.Quad,Enum.EasingDirection.Out), {CFrame=fwdCF})
                tween:Play() tween.Completed:Wait()
                local tool = player.Backpack:FindFirstChild("Hunter's Grasp")
                local remote = char and char:FindFirstChild("Communicate")
                if tool and remote then remote:FireServer({["Tool"]=tool,["Goal"]="Console Move"}) end
                isTweening = false
            end)
        elseif not isPlaying then lastPlaying = false end
    end)
end})

-- Upper + Grasp
local upperGraspConn = nil
AutoFeatSection:Toggle({ Title = "👊 Upper + Grasp", Default = false, Callback = function(enabled)
    if upperGraspConn then upperGraspConn:Disconnect() upperGraspConn = nil end
    if not enabled then return end
    local isTweening = false
    local lastPlaying = false
    local cooldown = false
    upperGraspConn = RunService.RenderStepped:Connect(function()
        local character = player.Character
        local hrp = character and character:FindFirstChild("HumanoidRootPart")
        local humanoid = character and character:FindFirstChild("Humanoid")
        if not character or not hrp or not humanoid then return end
        local isPlaying = false
        for _,track in ipairs(humanoid:GetPlayingAnimationTracks()) do
            if track.Animation and track.Animation.AnimationId=="rbxassetid://10503381238" then isPlaying=true break end
        end
        if isPlaying and not isTweening and not lastPlaying and not cooldown then
            isTweening=true lastPlaying=true cooldown=true
            task.delay(0.18, function()
                local target = nil
                local shortest = 7
                local live = workspace:FindFirstChild("Live")
                if live then
                    for _,model in ipairs(live:GetChildren()) do
                        if model:IsA("Model") and model~=character then
                            local torso = model:FindFirstChild("Torso") or model:FindFirstChild("UpperTorso")
                            if torso then
                                local dist = (hrp.Position-torso.Position).Magnitude
                                if dist <= shortest then shortest=dist target=torso end
                            end
                        end
                    end
                end
                if target then
                    local targetPos = target.Position + Vector3.new(0,8,0)
                    TweenService:Create(hrp, TweenInfo.new(0.1,Enum.EasingStyle.Linear), {CFrame=CFrame.new(targetPos)}):Play()
                end
                local tool = player.Backpack:FindFirstChild("Hunter's Grasp")
                local remote = character and character:FindFirstChild("Communicate")
                if tool and remote then remote:FireServer({["Tool"]=tool,["Goal"]="Console Move"}) end
                isTweening = false
                task.delay(15, function() cooldown = false end)
            end)
        elseif not isPlaying then lastPlaying = false end
    end)
end})

-- Whirlwind Dunk
AutoFeatSection:Toggle({ Title = "🌪️ Whirlwind Dunk", Default = false, Callback = function(isEnabled)
    if _G.TeleportAnimConnection then _G.TeleportAnimConnection:Disconnect() _G.TeleportAnimConnection = nil end
    if isEnabled then
        local isTeleporting = false
        local lastTrack = nil
        _G.TeleportAnimConnection = RunService.RenderStepped:Connect(function()
            local character = player.Character
            if not character or isTeleporting then return end
            local humanoid = character:FindFirstChildWhichIsA("Humanoid")
            local root = character:FindFirstChild("HumanoidRootPart")
            if not humanoid or not root then return end
            for _,track in ipairs(humanoid:GetPlayingAnimationTracks()) do
                if track.Animation and track.Animation.AnimationId=="rbxassetid://12296113986" then
                    if lastTrack==track then return end
                    lastTrack = track
                    isTeleporting = true
                    task.delay(1, function()
                        if root and root.Parent then root.CFrame = root.CFrame + Vector3.new(0,70,0) end
                        isTeleporting = false
                    end)
                    break
                end
            end
            if lastTrack and not lastTrack.IsPlaying then lastTrack = nil end
        end)
    end
end})

-- KIBA Tech
local kibaTechConn    = nil
local kibaTechEnabled = false
AutoFeatSection:Toggle({ Title = "⚔️ KIBA Tech", Default = false, Callback = function(state)
    kibaTechEnabled = state
    local animationIds = {["rbxassetid://10503381238"]=true,["rbxassetid://13379003796"]=true}
    local TWEEN_HEIGHT = Vector3.new(0,5,0)
    local function setupKiba(character)
        if kibaTechConn then kibaTechConn:Disconnect() end
        if not kibaTechEnabled then return end
        local isTweening = false
        local lastPlaying = false
        local cooldown = false
        kibaTechConn = RunService.RenderStepped:Connect(function()
            local hrp = character:FindFirstChild("HumanoidRootPart")
            local humanoid = character:FindFirstChild("Humanoid")
            if not character or not hrp or not humanoid then return end
            local isPlaying = false
            for _,track in ipairs(humanoid:GetPlayingAnimationTracks()) do
                if track.Animation and animationIds[track.Animation.AnimationId] then isPlaying=true break end
            end
            if isPlaying and not isTweening and not lastPlaying and not cooldown then
                isTweening=true lastPlaying=true cooldown=true
                task.delay(0.18, function()
                    local target = nil
                    local shortest = 15
                    local live = workspace:FindFirstChild("Live")
                    if live then
                        for _,model in ipairs(live:GetChildren()) do
                            if model:IsA("Model") and model~=character then
                                local root2 = model:FindFirstChild("HumanoidRootPart") or model:FindFirstChild("Head") or model:FindFirstChild("UpperTorso")
                                if root2 then
                                    local dist = (hrp.Position-root2.Position).Magnitude
                                    if dist<shortest then shortest=dist target=model end
                                end
                            end
                        end
                    end
                    if target and target:FindFirstChild("HumanoidRootPart") then
                        local targetPos = target.HumanoidRootPart.Position + TWEEN_HEIGHT
                        TweenService:Create(hrp, TweenInfo.new(0.1,Enum.EasingStyle.Linear), {CFrame=CFrame.new(targetPos)}):Play()
                        local remote = character:FindFirstChild("Communicate")
                        if remote then remote:FireServer({["Dash"]=Enum.KeyCode.W,["Key"]=Enum.KeyCode.Q,["Goal"]="KeyPress"}) end
                    end
                    isTweening = false
                    task.delay(4, function() cooldown = false end)
                end)
            elseif not isPlaying then lastPlaying = false end
        end)
    end
    if state and player.Character then setupKiba(player.Character) end
    player.CharacterAdded:Connect(function(char) if kibaTechEnabled then setupKiba(char) end end)
end})

-- ══════════════════════════════════════════
--  SERVER TAB
-- ══════════════════════════════════════════
local ServerSection = ServerTab:Section({ Title = "Server Tools", Shown = true })

ServerSection:Button({ Title = "⚡ Anti Lag", Callback = function()
    task.spawn(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/Antilag%20script"))()
    end)
end})

ServerSection:Button({ Title = "🔄 Rejoin", Callback = function()
    local TeleportService = game:GetService("TeleportService")
    local sg = Instance.new("ScreenGui", playerGui)
    sg.Name = "RejoinGui"
    local lbl = Instance.new("TextLabel", sg)
    lbl.Size = UDim2.new(0.3,0,0.2,0)
    lbl.Position = UDim2.new(0.35,0,0.4,0)
    lbl.TextScaled = true
    lbl.BackgroundTransparency = 1
    lbl.TextColor3 = Color3.new(1,1,1)
    lbl.Font = Enum.Font.GothamBold
    for i=5,1,-1 do lbl.Text="Rejoining in "..i.."..." task.wait(1) end
    sg:Destroy()
    TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, player)
end})

ServerSection:Button({ Title = "🌐 Server Hop", Callback = function()
    local TeleportService = game:GetService("TeleportService")
    local HttpService = game:GetService("HttpService")
    local placeId = game.PlaceId
    local sg = Instance.new("ScreenGui", playerGui)
    local lbl = Instance.new("TextLabel", sg)
    lbl.Size = UDim2.new(0.3,0,0.2,0)
    lbl.Position = UDim2.new(0.35,0,0.4,0)
    lbl.TextScaled = true
    lbl.BackgroundTransparency = 1
    lbl.TextColor3 = Color3.new(1,1,1)
    lbl.Font = Enum.Font.GothamBold
    for i=5,1,-1 do lbl.Text="Hopping in "..i.."..." task.wait(1) end
    sg:Destroy()
    local servers = {}
    pcall(function()
        local data = HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..placeId.."/servers/Public?sortOrder=Asc&limit=100"))
        if data and data.data then
            for _,s in pairs(data.data) do
                if s.playing < s.maxPlayers and s.id ~= game.JobId then table.insert(servers, s.id) end
            end
        end
    end)
    if #servers > 0 then TeleportService:TeleportToPlaceInstance(placeId, servers[math.random(1,#servers)], player) end
end})

ServerSection:Button({ Title = "👥 Small Server", Callback = function()
    local TeleportService = game:GetService("TeleportService")
    local HttpService = game:GetService("HttpService")
    local placeId = game.PlaceId
    local sg = Instance.new("ScreenGui", playerGui)
    local lbl = Instance.new("TextLabel", sg)
    lbl.Size = UDim2.new(0.3,0,0.2,0)
    lbl.Position = UDim2.new(0.35,0,0.4,0)
    lbl.TextScaled = true lbl.BackgroundTransparency = 1
    lbl.TextColor3 = Color3.new(1,1,1) lbl.Font = Enum.Font.GothamBold
    for i=5,1,-1 do lbl.Text="Finding small server in "..i.."..." task.wait(1) end
    sg:Destroy()
    local servers = {}
    local cursor = ""
    for i=1,5 do
        local url = "https://games.roblox.com/v1/games/"..placeId.."/servers/Public?sortOrder=Asc&limit=100"
        if cursor ~= "" then url = url.."&cursor="..cursor end
        local ok, resp = pcall(function() return HttpService:JSONDecode(game:HttpGet(url)) end)
        if ok and resp and resp.data then
            for _,s in pairs(resp.data) do
                if s.playing < 5 and s.id ~= game.JobId then table.insert(servers, s.id) end
            end
            cursor = resp.nextPageCursor or ""
            if cursor == "" then break end
        else break end
    end
    if #servers > 0 then TeleportService:TeleportToPlaceInstance(placeId, servers[math.random(1,#servers)], player) end
end})

ServerSection:Button({ Title = "📊 Check Ping", Callback = function()
    local sg = Instance.new("ScreenGui", playerGui)
    sg.Name = "PingDisplay" sg.ResetOnSpawn = false
    local lbl = Instance.new("TextLabel", sg)
    lbl.Size = UDim2.new(0,150,0,40)
    lbl.Position = UDim2.new(1,-160,0,10)
    lbl.BackgroundTransparency = 0.4
    lbl.BackgroundColor3 = Color3.fromRGB(0,0,0)
    lbl.TextColor3 = Color3.new(1,1,1)
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 20
    lbl.Text = "PING: -- ms"
    task.spawn(function()
        while lbl and lbl.Parent do
            local ping = math.floor(player:GetNetworkPing()*1000)
            lbl.Text = "PING: "..ping.." ms"
            if ping <= 80 then lbl.TextColor3 = Color3.fromRGB(0,255,0)
            elseif ping <= 150 then lbl.TextColor3 = Color3.fromRGB(255,170,0)
            else lbl.TextColor3 = Color3.fromRGB(255,0,0) end
            task.wait(1)
        end
    end)
end})

ServerSection:Toggle({ Title = "🚫 Anti Kick", Default = false, Callback = function(state)
    if state then
        pcall(function()
            local mt = getrawmetatable(game)
            local oldNamecall = mt.__namecall
            setreadonly(mt, false)
            mt.__namecall = newcclosure(function(self,...)
                if getnamecallmethod()=="Kick" and self==player then return nil end
                return oldNamecall(self,...)
            end)
            player.Kick = function() return nil end
            setreadonly(mt, true)
        end)
    end
end})

-- ══════════════════════════════════════════
--  AUTO BLOCK TAB
-- ══════════════════════════════════════════
local AutoBlockSection = AutoBlockTab:Section({ Title = "Auto Block Scripts", Shown = true })
AutoBlockSection:Button({ Title = "🛡️ Notpaki Block", Callback = function()
    task.spawn(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Cyborg883/TSB/refs/heads/main/CombatGui"))()
    end)
end})
AutoBlockSection:Button({ Title = "🛡️ YQANTG Block", Callback = function()
    task.spawn(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Kietba/Kietba/refs/heads/main/YQANTG%20AUTO%20BLOCK%20(1).txt"))()
    end)
end})

-- ══════════════════════════════════════════
--  ADMIN TAB
-- ══════════════════════════════════════════
local AdminSection = AdminTab:Section({ Title = "Admin Scripts", Shown = true })
AdminSection:Button({ Title = "♾️ Infinite Yield", Callback = function()
    task.spawn(function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"))()
    end)
end})
AdminSection:Button({ Title = "👑 Nameless Admin", Callback = function()
    task.spawn(function()
        loadstring(game:HttpGet("https://github.com/FilteringEnabled/NamelessAdmin/blob/main/Source?raw=true"))()
    end)
end})

-- ══════════════════════════════════════════
--  SETTINGS TAB  (includes Close button)
-- ══════════════════════════════════════════
local UISection = SettingsTab:Section({ Title = "Interface", Shown = true })

UISection:Keybind({
    Title   = "Toggle Hub",
    Default = Enum.KeyCode.RightShift,
    Callback = function()
        Window:Toggle()
    end,
})

local ControlSection = SettingsTab:Section({ Title = "Controls", Shown = true })

ControlSection:Button({
    Title = "❌ Close / Destroy Hub",
    Callback = function()
        WindUI:Notify({ Title="Molten Hub", Content="Closing hub... 🔥", Duration=2, Icon="x-circle" })
        task.wait(0.5)
        Window:Destroy()
    end,
})

-- ══════════════════════════════════════════
--  XCZ FUNCTION
-- ══════════════════════════════════════════
local function doXCZ()
    if xczCooldown then return end
    xczCooldown = true
    local char = player.Character
    if not char then xczCooldown=false return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then xczCooldown=false return end
    VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.Four,false,game) task.wait(0.05)
    VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.Four,false,game) task.wait(0.1)
    VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.W,false,game) task.wait(0.01)
    VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.Q,false,game) task.wait(0.03)
    VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.Q,false,game) task.wait(0.04)
    hrp.CFrame = hrp.CFrame*CFrame.Angles(0,math.pi,0) task.wait(0.03)
    VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.W,false,game) task.wait(0.05)
    VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.One,false,game) task.wait(0.02)
    VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.One,false,game) task.wait(0.5)
    xczCooldown = false
end

UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode==Enum.KeyCode.E and xczEnabled then task.spawn(doXCZ) end
end)

-- ══════════════════════════════════════════
--  M1 SPAM LOOP
-- ══════════════════════════════════════════
task.spawn(function()
    local punchCount = 0
    while true do
        if m1Spam then
            local char = player.Character
            if char then
                local screenSize = workspace.CurrentCamera.ViewportSize
                local punchX = screenSize.X*0.93
                local punchY = screenSize.Y*0.50
                VirtualInputManager:SendMouseButtonEvent(punchX,punchY,0,true,game,1) task.wait(0.05)
                VirtualInputManager:SendMouseButtonEvent(punchX,punchY,0,false,game,1)
                VirtualInputManager:SendKeyEvent(true,Enum.KeyCode.One,false,game) task.wait(0.02)
                VirtualInputManager:SendKeyEvent(false,Enum.KeyCode.One,false,game)
                punchCount += 1
                if xczEnabled and not xczCooldown and punchCount >= 4 then
                    punchCount = 0 task.spawn(doXCZ)
                end
            end
            task.wait(m1Delay)
        else punchCount=0 task.wait(0.05) end
    end
end)

-- ══════════════════════════════════════════
--  LOOP GOTO HEARTBEAT
-- ══════════════════════════════════════════
RunService.Heartbeat:Connect(function()
    if loopGoto and loopTargetName ~= "" then
        local target = Players:FindFirstChild(loopTargetName)
        if target and target.Character then
            local targetHRP = target.Character:FindFirstChild("HumanoidRootPart")
            local myHRP = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
            if targetHRP and myHRP then myHRP.CFrame = targetHRP.CFrame*CFrame.new(0,0,-2) end
        end
    end
end)

-- ══════════════════════════════════════════
--  KEY SPAM LOOP
-- ══════════════════════════════════════════
task.spawn(function()
    while true do
        if keySpam then
            local keys = {Enum.KeyCode.One,Enum.KeyCode.Two,Enum.KeyCode.Three,Enum.KeyCode.Four}
            for _,k in ipairs(keys) do
                if not keySpam then break end
                VirtualInputManager:SendKeyEvent(true,k,false,game) task.wait(0.02)
                VirtualInputManager:SendKeyEvent(false,k,false,game) task.wait(spamDelay)
            end
        else task.wait(0.05) end
    end
end)

-- ══════════════════════════════════════════
--  CHARACTER SETUP
-- ══════════════════════════════════════════
local function setupChar(char)
    if not char then return end
    local hum = char:WaitForChild("Humanoid")
    local hrp = char:WaitForChild("HumanoidRootPart")
    if deathTP and lastDeathCFrame then task.wait(0.15) hrp.CFrame = lastDeathCFrame end
    hum.Died:Connect(function()
        lastDeathCFrame = hrp.CFrame
        if autoReset then task.wait(resetDelay) player:LoadCharacter() end
    end)
end
if player.Character then setupChar(player.Character) end
player.CharacterAdded:Connect(setupChar)

-- ══════════════════════════════════════════
--  STARTUP NOTIFICATION
-- ══════════════════════════════════════════
WindUI:Notify({
    Title      = "🔥 MOLTEN HUB",
    Content    = "Script loaded successfully!",
    SubContent = "THE STRONGEST BATTLEGROUNDS",
    Duration   = 5,
    Icon       = "flame",
})
